# DNS 中继服务器 — 代码导读

> 按文件逐个讲解每个部分的作用。**函数只在第一次出现时解释**，后面直接说"调用 XX 函数"

---

## 目录

1. [Makefile — 构建系统](#1-makefile--构建系统)
2. [src/platform.h — 平台抽象层](#2-srcplatformh--平台抽象层)
3. [src/debug.h — 调试输出](#3-srcdebugh--调试输出)
4. [src/dns.h — DNS 协议结构体定义](#4-srcdnsh--dns-协议结构体定义)
5. [src/dns.c — DNS 报文编解码实现](#5-srcdnsc--dns-报文编解码实现)
6. [src/table.h — 域名-IP 对照表接口](#6-srctableh--域名-ip-对照表接口)
7. [src/table.c — 域名-IP 对照表实现](#7-srctablec--域名-ip-对照表实现)
8. [src/relay.h — 中继转发模块接口](#8-srcrelayh--中继转发模块接口)
9. [src/relay.c — 中继转发模块实现](#9-srcrelayc--中继转发模块实现)
10. [src/cache.h — 动态缓存接口](#10-srccacheh--动态缓存接口)
11. [src/cache.c — 动态缓存实现](#11-srccachec--动态缓存实现)
12. [src/main.c — 主程序](#12-srcmainc--主程序)

---

## 1. Makefile — 构建系统

**文件位置**：`Makefile`

**作用**：定义编译规则，支持 Windows（MinGW/MSVC）和 Linux。

```
CC = gcc                     # 编译器
CFLAGS = -Wall -Wextra ...   # 编译选项：开所有警告、C99标准、O2优化
SRC = src/main.c src/dns.c   # 所有源文件
...                          + src/table.c src/relay.c src/cache.c
```

| 命令 | 作用 |
|------|------|
| `make` | 编译整个项目 |
| `make clean` | 删除中间文件 |
| `make run` | 编译并运行 |

**要点**：
- Windows 额外链接 `ws2_32`（Winsock 库）
- Linux 不需要额外链接
- `-I src` 让编译器找 `#include "xxx.h"` 时去 `src/` 目录

---

## 2. src/platform.h — 平台抽象层

**作用**：把 Windows 和 Linux 的 socket 编程差异藏在这里，其他代码不用管在哪个系统。

### 开头 — 平台检测

```c
#if defined(_WIN32) || defined(_WIN64)
    #define PLATFORM_WIN 1      // Windows 标记
#elif defined(__linux__) || defined(__unix__)
    #define PLATFORM_POSIX 1     // Linux 标记
#endif
```

### Windows 分支

引入 `winsock2.h`、`windows.h`、`ws2tcpip.h` 这三个头文件（Windows socket 编程必须）。

定义三个平台函数/宏：

| 名字 | 作用 |
|------|------|
| `socket_init()` | 调用 `WSAStartup()` 初始化 Windows socket 子系统 |
| `socket_cleanup()` | 调用 `WSACleanup()` 清理 |
| `socket_close(s)` | 宏，展开为 `closesocket(s)`（Windows 关闭 socket 用这个，不是 close） |
| `socket_errno()` | 宏，展开为 `WSAGetLastError()`（取错误码） |

还定义了 `SOCKET_EWOULDBLOCK` 和 `SOCKET_ETIMEDOUT` 两个错误码常量。

### Linux 分支

引入 POSIX 标准头文件：`sys/socket.h`、`netinet/in.h`、`arpa/inet.h`、`unistd.h`、`fcntl.h`、`errno.h`、`sys/select.h`。

对应的平台函数：

| 名字 | 作用 |
|------|------|
| `socket_init()` | 空操作，直接返回 0（Linux 不需要初始化） |
| `socket_cleanup()` | 空操作 |
| `socket_close(s)` | 宏，展开为 `close(s)` |
| `socket_errno()` | 宏，展开为 `errno` |

额外定义了 `SOCKET` 为 `int`、`INVALID_SOCKET` 为 `-1`（Windows 的 `SOCKET` 类型在 Linux 下就是普通 int）。

### 结尾 — 通用部分

引入三个标准类型头文件：`stdint.h`（uint8_t/uint16_t）、`stdbool.h`（bool）、`stddef.h`（size_t）。

定义 DNS 协议常量：

```c
#define DNS_PORT            53       // DNS 标准端口
#define MAX_DNS_PACKET      512      // DNS 报文最大长度（RFC 1035 规定）
#define DNS_DEFAULT_TIMEOUT_MS 3000  // 等待上游 DNS 的超时（毫秒）
#define DNS_MAX_PENDING     1024     // 最多并发挂起查询数
```

---

## 3. src/debug.h — 调试输出

**作用**：提供分级调试输出，通过 `-d` / `-dd` 命令行参数控制详细程度。

### 调试级别枚举

```c
typedef enum {
    DEBUG_NONE    = 0,   // 无输出
    DEBUG_BASIC   = 1,   // -d: 时间/客户端IP/域名
    DEBUG_VERBOSE = 2    // -dd: 详细信息
} debug_level_t;
```

### 全局变量声明

```c
extern debug_level_t g_debug_level;
```

这个变量在 `main.c` 中定义，`-d` 或 `-dd` 会设置它的值。所有调试宏都检查这个值来决定是否输出。

### debug_timestamp() — 时间戳函数

返回当前时间的 `"HH:MM:SS"` 格式字符串。static inline 函数，每次调用都重新获取时间。

### 三个输出宏

| 宏 | 条件 | 用途 |
|----|------|------|
| `DEBUG_BASIC(fmt, ...)` | `g_debug_level >= 1` | 基本运行信息 |
| `DEBUG_VERBOSE(fmt, ...)` | `g_debug_level >= 2` | 详细调试信息 |
| `DEBUG_ERROR(fmt, ...)` | 无条件 | 错误信息（总是显示） |

所有宏都输出到 `stderr`，格式为 `[时间戳] 消息`。

---

## 4. src/dns.h — DNS 协议结构体定义

**作用**：定义 DNS 报文的数据结构（严格按 RFC 1035 §4.1），声明所有 DNS 编解码函数。

### PACKED 对齐

```c
#ifdef _MSC_VER
    #pragma pack(push, 1)
    #define PACKED
#else
    #define PACKED __attribute__((packed))
#endif
```

确保结构体在内存中是紧凑排列的（1 字节对齐），因为 DNS 报文是逐字节排列的，不能有填充字节。

### dns_header_t — DNS 头部结构体（12 字节）

```c
typedef struct PACKED {
    uint16_t id;          // 查询标识符：客户端设的，服务器原样返回
    uint16_t flags;       // 标志位：QR/Opcode/AA/TC/RD/RA/Z/RCODE
    uint16_t qdcount;     // Question 段中的问题数
    uint16_t ancount;     // Answer 段中的资源记录数
    uint16_t nscount;     // Authority 段中的记录数
    uint16_t arcount;     // Additional 段中的记录数
} dns_header_t;
```

**内存布局**（12 字节，网络字节序 Big-Endian）：

```
 0-1:  ID
 2-3:  Flags
 4-5:  QDCOUNT
 6-7:  ANCOUNT
 8-9:  NSCOUNT
10-11: ARCOUNT
```

### Flags 位域操作宏

因为 `flags` 是一个 `uint16_t`，里面塞了多个位字段，所以用宏来读写每个位。

| 宏 | 位 | 作用 |
|----|----|------|
| `DNS_GET_QR(h)` | 15 | 取 QR：0=查询，1=响应 |
| `DNS_GET_OPCODE(h)` / `DNS_SET_OPCODE(h,v)` | 14-11 | 操作码 |
| `DNS_SET_AA(h,v)` | 10 | 设置权威回答标志 |
| `DNS_GET_TC(h)` | 9 | 取截断标志 |
| `DNS_GET_RD(h)` / `DNS_SET_RD(h,v)` | 8 | 期望递归 |
| `DNS_SET_RA(h,v)` | 7 | 递归可用 |
| `DNS_GET_RCODE(h)` / `DNS_SET_RCODE(h,v)` | 3-0 | 响应码 |

关键的响应码常量：
```c
#define DNS_RCODE_NOERROR   0  // 成功
#define DNS_RCODE_NXDOMAIN  3  // 域名不存在
```

### QTYPE / QCLASS 常量

```c
#define DNS_TYPE_A     1   // IPv4 地址
#define DNS_TYPE_AAAA  28  // IPv6 地址
#define DNS_TYPE_CNAME 5   // 别名
#define DNS_TYPE_MX    15  // 邮件交换
#define DNS_TYPE_PTR   12  // 反向查询
#define DNS_TYPE_NS    2   // 域名服务器
#define DNS_TYPE_ANY   255 // 任意类型

#define DNS_CLASS_IN   1   // Internet 类
```

### dns_rr_t — 资源记录结构体

```c
typedef struct PACKED {
    uint16_t type;      // A/AAAA/MX/CNAME...
    uint16_t cls;       // 通常为 IN(1)
    uint32_t ttl;       // 生存时间（秒）
    uint16_t rdlength;  // RDATA 的长度
    // 后面紧跟 rdlength 字节的 RDATA（IP 地址/MX 交换器...）
} dns_rr_t;
```

注意：这个结构体不包含 NAME 字段，因为 DNS 的 NAME 是变长的（长度前缀或压缩指针），不能固定放在结构体里。

### 函数声明（只列原型，具体实现见 dns.c）

以下函数都声明在这里，实现都在 `dns.c` 中：

| 函数 | 一句话说明 |
|------|----------|
| `dns_encode_name()` | 把 "www.bupt.cn" 编码成 DNS 格式 "\x03www\x05bupt\x03edu\x02cn\x00" |
| `dns_decode_name()` | 把 DNS 格式解码回点分字符串，支持压缩指针 |
| `dns_skip_name()` | 跳过报文中的域名（不解码），返回值跳过后的位置 |
| `dns_build_response()` | 根据客户端的查询报文构造响应报文 |
| `dns_extract_question()` | 从查询报文中提取域名和查询类型 |
| `dns_build_relay_query()` | 复制查询报文，只改 ID 字段（用于转发） |
| `dns_restore_id()` | 把响应报文中的 ID 改回客户端的原始 ID |
| `dns_extract_a_record()` | 从上游响应中提取第一条 A 记录的 IP 地址 |

---

## 5. src/dns.c — DNS 报文编解码实现

**作用**：实现 `dns.h` 中声明的所有函数。

### dns_encode_name() — 域名编码

```c
int dns_encode_name(uint8_t *dst, size_t dst_len, const char *name);
```

**做什么**：把 `"www.bupt.edu.cn"` 编码成 `\x03www\x05bupt\x03edu\x02cn\x00`。

**编码规则**：
- 每个标签（两个点之间的部分）前面加一个字节表示长度
- 标签长度不超过 63 字节
- 末尾加 `\x00` 表示根标签（结束）

**怎么编**：遍历字符串，找 `.` 分割标签。每个标签先写长度字节，再写标签内容。最后写 `\x00`。

### dns_decode_name() — 域名解码

```c
int dns_decode_name(char *dst, size_t dst_len,
                    const uint8_t *src,
                    const uint8_t *msg, size_t msg_len);
```

**做什么**：把 DNS 报文中的编码域名解码回点分字符串。

**关键特性 — 压缩指针**：DNS 为了节省空间，如果同一个域名在报文中出现多次（比如 Question 段和 Answer 段），后面的会用**指针**指向前面的位置。如果字节的最高两位是 `11`（即 `>= 0xC0`），那这个字节和后一个字节拼起来就是一个偏移量，指向报文中的另一个位置。

**怎么解**：读一个字节，如果 `>= 0xC0` 就做指针跳转；否则当作长度字节，读这么多个字符，加个 `.`，继续。

**返回值**：消耗的字节数（不是域名长度），上层用这个值跳转到下一个字段。

### dns_skip_name() — 跳过域名

```c
const uint8_t *dns_skip_name(const uint8_t *p, const uint8_t *end);
```

**做什么**：不解码，只是跳过报文中的一个域名，返回跳过后指向下一个字段的位置。

**实现**：和解码一样遍历标签，遇到压缩指针就跳 2 字节返回。用来在解析报文时定位到 QTYPE 或 Answer 段的 RR 开头。

### dns_extract_question() — 提取查询内容

```c
int dns_extract_question(const uint8_t *query, size_t query_len,
                          char *domain, size_t domain_len,
                          uint16_t *qtype);
```

**做什么**：从客户端发来的 DNS 查询报文中提取**域名**和**查询类型**。

**流程**：
1. 检查报文长度 ≥ 12 字节（头部大小）
2. 读取 `qdcount`（问题数），必须 ≥ 1
3. 跳到头部后面的 Question 段起始位置
4. 调用 `dns_decode_name()` 解码域名
5. 读 2 字节 QTYPE + 2 字节 QCLASS

**用在**：`main.c` 的 `handle_client_query()` 中，拿到域名后去查表或转发。

### dns_build_response() — 构建响应报文

```c
int dns_build_response(const uint8_t *query, size_t query_len,
                        uint8_t *response, size_t resp_len,
                        uint32_t ip_addr, uint32_t ttl);
```

**做什么**：根据客户端查询构建 DNS 响应。这个函数是**本地解析和拦截功能的核心**。

**参数**：
- `ip_addr = 0`：返回 NXDOMAIN（域名不存在）— 用于拦截
- `ip_addr = 有效 IP`：返回该 IP 的 A 记录 — 用于本地解析

**构建过程**：
1. **复制头部**，修改 flags：
   - 设 QR=1（标记为响应）
   - 继承客户端的 RD 位
   - 设 RA=1（递归可用）
   - 如果 `ip_addr==0`，设 RCODE=3(NXDOMAIN)，否则 RCODE=0
2. **复制 Question 段**（原封不动带回给客户端）
3. 如果 `ip_addr==0`，到此为止（NXDOMAIN 不需要 Answer）
4. **添加 Answer 段**：
   - NAME 用**压缩指针**指向 Question 段中的域名（偏移 = 12 字节，即 sizeof(dns_header_t)）
   - TYPE = A(1)，CLASS = IN(1)，TTL = 参数值
   - RDLENGTH = 4，RDATA = ip_addr 的 4 字节

**DNS 名称压缩**：`0xC000 | 12` 表示"从报文偏移 12 字节处开始读域名"——12 正好是头部长度，Question 段从第 12 字节开始。

### dns_build_relay_query() — 构建转发报文

```c
int dns_build_relay_query(const uint8_t *query, size_t query_len,
                           uint16_t new_id,
                           uint8_t *out, size_t out_len);
```

**做什么**：复制客户端的查询报文，只改 ID 字段为 `new_id`。用于中继转发——用新的 proxy_id 替代客户端的原始 ID。

**实现**：`memcpy` 整个报文，然后 `hdr->id = htons(new_id)`。

### dns_restore_id() — 恢复原始 ID

```c
int dns_restore_id(uint8_t *response, size_t response_len,
                    uint16_t original_id);
```

**做什么**：把上游返回的响应报文中的 ID 改回客户端的原始 ID。

**用在**：`relay.c` 的 `relay_process_response()` 中，在把响应转发给客户端之前调用。

### dns_extract_a_record() — 提取 A 记录

```c
int dns_extract_a_record(const uint8_t *response, size_t resp_len,
                          char *domain, size_t domain_len,
                          uint32_t *ip_addr, uint32_t *ttl);
```

**做什么**：从上游返回的 DNS 响应中提取第一条 A 记录的 IP 地址。

**流程**：
1. 检查是响应报文（QR=1）且无错误（RCODE=0）
2. 跳过 Question 段（可能有多个 question）
3. 定位到第一个 Answer，调用 `dns_decode_name()` 读域名
4. 读 RR 的 type，必须是 A(1)
5. 读 RDLENGTH 必须是 4，然后读 4 字节的 IP 地址

**返回值**：1=找到 A 记录，0=没有 A 记录（CNAME/MX/AAAA 等），-1=解析失败。

**用在**：`main.c` 的 `handle_upstream_response()` 中，提取 IP 后调用 `cache_put()` 缓存。

---

## 6. src/table.h — 域名-IP 对照表接口

**作用**：声明对照表的数据结构和操作函数。

### 数据结构

```c
#define TABLE_HASH_SIZE 8191  // 哈希桶数（质数）

typedef struct table_entry {
    char *domain;              // 域名
    uint32_t ip_addr;          // IP 地址（网络字节序）
    struct table_entry *next;  // 链表指针（处理哈希冲突）
} table_entry_t;

typedef struct {
    table_entry_t *buckets[TABLE_HASH_SIZE];  // 哈希桶数组
    size_t count;                              // 总记录数
    char *filename;                            // 文件名
} table_t;
```

### 四个函数

| 函数 | 作用 |
|------|------|
| `table_init(&tbl)` | 清零对照表，所有桶置 NULL |
| `table_load_file(&tbl, filename)` | 从文件读入域名-IP 对，构造哈希表 |
| `table_lookup(&tbl, domain, &ip)` | 查域名，找到返回 1 并写 ip，没找到返回 0 |
| `table_destroy(&tbl)` | 释放所有 malloc 的内存 |

---

## 7. src/table.c — 域名-IP 对照表实现

### hash_str() — 哈希函数（静态）

```c
static unsigned long hash_str(const char *str)
```

djb2 算法：`hash = ((hash << 5) + hash) + c`，即 `hash * 33 + c`。对字符转小写再算（域名不区分大小写）。

### table_init() — 初始化

```c
void table_init(table_t *tbl)
```

`memset` 清零整个结构体。

### table_load_file() — 从文件加载

```c
int table_load_file(table_t *tbl, const char *filename)
```

**做什么**：逐行读取 `dnsrelay.txt`，解析 `IP 域名` 对，放入哈希表。

**文件格式**：
```
0.0.0.0 ad1.sina.com.cn       ← 拦截
123.127.134.10 www.bupt.cn    ← 正常解析
# 这是注释                       ← 跳过
                               ← 空行也跳过
```

**处理流程**：
1. 打开文件
2. 逐行读入（`fgets`）
3. 每行先跳前导空白，跳过空行和 `#` 注释
4. 读 IP 字符串直到遇到空白
5. 读域名直到遇到空白或换行
6. `inet_pton()` 将字符串 IP 转为二进制（网络字节序）
7. 调用 `hash_str()` 算哈希，拿到桶索引
8. `malloc` 一个 `table_entry_t`，**头插法**插入链表

**头插法**：`entry->next = buckets[idx]; buckets[idx] = entry;` 新记录插在链表头部。

### table_lookup() — 在表中查找

```c
int table_lookup(const table_t *tbl, const char *domain, uint32_t *ip_addr)
```

1. 调用 `hash_str(domain)` 算哈希
2. 遍历对应桶的链表
3. 每个节点用 `_stricmp()`（Windows）或 `strcasecmp()`（Linux）做**不区分大小写**的比较
4. 找到就写 `*ip_addr` 并返回 1，找不到返回 0

**注意**：返回 1 时 `ip_addr` 可能是 0（表示拦截），返回 0 时 `ip_addr` 值无效。

### table_destroy() — 销毁

```c
void table_destroy(table_t *tbl)
```

遍历所有桶的链表，`free` 每个节点的 `domain` 和节点本身，最后 `free` 文件名。

---

## 8. src/relay.h — 中继转发模块接口

**作用**：声明中继转发的数据结构和函数。

### 设计思想

当客户端查询的域名不在本地对照表中时，需要转发到上游 DNS。如果同时有多个客户端在查，每个查询有各自的 ID，直接转发会导致 ID 冲突。所以需要做 **ID 映射**。

### 数据结构

```c
#define MAX_PENDING 1024      // 最多同时挂起的查询数
#define PENDING_TIMEOUT 5     // 超时秒数

typedef struct {
    bool    used;              // 是否已被占用
    uint16_t proxy_id;         // 我们分配给这个查询的代理 ID
    struct sockaddr_in client_addr;  // 客户端的网络地址
    socklen_t client_len;      // 地址长度
    uint16_t original_id;      // 客户端原始的查询 ID
    time_t  timestamp;         // 发送时间（用于检测超时）
    uint8_t query[MAX_DNS_PACKET];  // 原始查询报文（做日志用）
    size_t  query_len;
} pending_query_t;

typedef struct {
    SOCKET  server_sock;       // 服务器 socket（发响应给客户端）
    SOCKET  upstream_sock;     // 上游 socket（与外部 DNS 通信）
    struct sockaddr_in upstream_addr;  // 上游 DNS 地址
    pending_query_t pending[MAX_PENDING];  // 挂起查询数组
    uint16_t next_proxy_id;    // 下一个可用 proxy_id
} relay_ctx_t;
```

### 六个函数

| 函数 | 作用 |
|------|------|
| `relay_init()` | 创建上游 socket，存上游地址 |
| `relay_forward()` | 分配 proxy_id，改 ID 后转发，记录映射 |
| `relay_process_response()` | 收到上游响应，查映射表，恢复 ID，发回客户端 |
| `relay_check_timeouts()` | 遍历挂起表，超 5 秒的清理掉 |
| `relay_get_upstream_sock()` | 返回上游 socket fd（给 select 用） |
| `relay_destroy()` | 关闭上游 socket |

---

## 9. src/relay.c — 中继转发模块实现

### relay_init() — 初始化

```c
int relay_init(relay_ctx_t *ctx, SOCKET server_sock,
               const char *upstream_ip, uint16_t upstream_port)
```

1. 清零 `ctx`
2. 存 `server_sock`（后面发响应给客户端要用）
3. 创建 `upstream_sock`（UDP），设非阻塞
4. 用 `inet_pton()` 把字符串 IP 转成二进制地址
5. 存 `upstream_addr`（目标就是上游 DNS）

### relay_forward() — 转发查询

```c
int relay_forward(relay_ctx_t *ctx, const uint8_t *query, size_t query_len,
                  const struct sockaddr_in *client_addr, socklen_t client_len)
```

**流程**：
1. 读客户端的原始 ID：`ntohs(hdr->id)`
2. 遍历 `pending[]` 找一个空闲槽位（`used == false`）
3. 如果满了（1024 个都在用），丢弃这个查询
4. 分配 `proxy_id`：`next_proxy_id++`，检查不跟已有的冲突
5. 调用 `dns_build_relay_query()` 复制报文并替换 ID
6. 用 `sendto()` 通过 `upstream_sock` 发给上游 DNS
7. 记录映射：`pending[slot]` 填入 proxy_id、客户端地址、原始 ID、时间戳、原始查询

返回 proxy_id（> 0）表示成功，-1 表示失败。

### relay_process_response() — 处理上游响应

```c
int relay_process_response(relay_ctx_t *ctx,
                           const uint8_t *response, size_t response_len)
```

**流程**：
1. 如果是 `response == NULL`，直接返回 0（用来快速检查冲突）
2. 读响应的 ID（就是当初我们设的 proxy_id）
3. 遍历 `pending[]`，找 `proxy_id` 匹配的条目
4. 没找到：说明已经超时了，忽略
5. 找到了：
   - 复制响应报文
   - 调用 `dns_restore_id()` 把 ID 改回客户端的 `original_id`
   - 调用 `sendto()` 通过 `server_sock` 发给客户端
   - 打日志（调用 `dns_extract_question()` 从保存的原始查询中提取域名）
   - 标记 `pending[slot].used = false`（释放槽位）

### relay_check_timeouts() — 超时检查

```c
void relay_check_timeouts(relay_ctx_t *ctx)
```

遍历所有 `pending`，如果 `difftime(now, timestamp) >= PENDING_TIMEOUT`（5 秒），就标记为未使用。超时不通知客户端（符合 DNS 协议——不给你回你也不知道）。

### relay_get_upstream_sock() / relay_destroy()

`relay_get_upstream_sock()` 返回 `upstream_sock`，给 `main.c` 的 `select()` 用。

`relay_destroy()` 调用 `socket_close()` 关掉 `upstream_sock`。

---

## 10. src/cache.h — 动态缓存接口

**作用**：声明缓存的存储结构和操作函数。

### 数据结构

```c
#define CACHE_HASH_SIZE 1021  // 哈希桶数（质数）

typedef enum {
    CACHE_VALID,      // 有效的正常记录
    CACHE_EXPIRED,    // 已过期
    CACHE_NEGATIVE    // 否定缓存（域名不存在）
} cache_status_t;

typedef struct cache_entry {
    char domain[256];           // 域名（直接存数组，不需额外 malloc）
    uint32_t ip_addr;           // IP 地址，0 表示 NXDOMAIN
    time_t  expire_time;        // 过期时间（绝对时间）
    cache_status_t status;
    struct cache_entry *next;
} cache_entry_t;

typedef struct {
    cache_entry_t *buckets[CACHE_HASH_SIZE];
    size_t count;                // 当前有效条目数
    size_t max_entries;          // 容量上限
} dns_cache_t;
```

### 五个函数

| 函数 | 作用 |
|------|------|
| `cache_init()` | 清零缓存，设容量上限 |
| `cache_put()` | 存入一条域名→IP 映射，带 TTL |
| `cache_get()` | 查缓存，没过期就返回 IP |
| `cache_cleanup()` | 遍历所有桶，删掉过期的条目 |
| `cache_destroy()` | 释放所有 malloc 内存 |

---

## 11. src/cache.c — 动态缓存实现

### cache_hash() — 哈希函数

同 `table.c` 的 `hash_str()`，也是 djb2 算法。注意这里叫 `cache_hash` 不是 `hash_str`，因为是静态函数不同文件不冲突。

### cache_init() — 初始化

```c
void cache_init(dns_cache_t *cache, size_t max_entries)
```

清零整个结构体，设容量上限（默认 10000，实际传 5000）。

### cache_put() — 放入缓存

```c
void cache_put(dns_cache_t *cache, const char *domain,
               uint32_t ip_addr, uint32_t ttl)
```

1. **TTL 下限**：如果上游给的 TTL < 60 秒，强制设成 60 秒（避免刚缓存就过期）
2. **计算过期时间**：`expire = time(NULL) + ttl`
3. **算哈希**：`cache_hash(domain)`
4. **如果已有**（遍历链表，不区分大小写比较）：更新 IP、过期时间、状态
5. **如果满了**：先调 `cache_cleanup()` 清理过期条目，还满就放弃缓存
6. **创建新节点**：`malloc`，头插法插入

### cache_get() — 取缓存

```c
int cache_get(dns_cache_t *cache, const char *domain, uint32_t *ip_addr)
```

1. 算哈希，遍历链表找域名
2. 如果 `now >= expire_time`：标记过期，返回 0
3. 如果没过期：写 `*ip_addr`，返回 1

### cache_cleanup() — 清理过期条目

```c
size_t cache_cleanup(dns_cache_t *cache)
```

遍历所有桶的链表，**用二级指针**（`cache_entry_t **pp`）遍历，如果过期就 `*pp = e->next; free(e)`。返回删了多少条。

**为什么用二级指针**：单向链表删除时，需要知道**前一个节点**的 next 指针。二级指针 `pp` 一直指向"当前应该指向这个节点"的指针位置，省掉了 `prev` 变量。

### cache_destroy() — 销毁

遍历所有桶，逐节点 `free`。

---

## 12. src/main.c — 主程序

**作用**：程序入口，把以上所有模块串起来。

### 包含的头文件

```c
#include "platform.h"   // 平台抽象
#include "dns.h"        // DNS 编解码
#include "table.h"      // 对照表
#include "relay.h"      // 中继转发
#include "cache.h"      // 动态缓存
#include "debug.h"      // 调试输出
```

### 全局变量

```c
debug_level_t g_debug_level = DEBUG_NONE;  // 调试级别，默认关闭
```

### config_t — 配置结构体

```c
typedef struct {
    char    upstream_ip[64];   // 上游 DNS IP（如 "8.8.8.8"）
    uint16_t upstream_port;    // 上游端口（默认 53）
    char    table_file[256];   // 对照表文件路径
    table_t table;             // 对照表（table.h 的类型）
    relay_ctx_t relay;         // 中继模块（relay.h 的类型）
    dns_cache_t cache;         // 缓存（cache.h 的类型）
    SOCKET  server_sock;       // 监听 socket
} config_t;
```

这是一个"大袋子"，把所有状态装在一起，方便在函数之间传递。

### main() — 主函数

**完整流程**：

```
① SetConsoleOutputCP(CP_UTF8)    ← 仅 Windows，设控制台编码
② socket_init()                  ← 初始化 Winsock（Windows 需要）
③ parse_args()                   ← 解析 -d/-dd 和 IP/文件名
④ table_init() + table_load_file()  ← 加载域名对照表
⑤ init_server()                  ← 创建 UDP socket，绑端口 53
⑥ cache_init()                   ← 初始化动态缓存，容量 5000
⑦ relay_init()                   ← 创建上游 socket
⑧ 进入 select() 事件循环
```

### parse_args() — 参数解析

```c
static int parse_args(config_t *cfg, int argc, char *argv[])
```

**命令行格式**：`dnsrelay [-d|-dd] [dns-server-ipaddr] [filename]`

遍历 `argv`：
- `-d` → `g_debug_level = DEBUG_BASIC`
- `-dd` → `g_debug_level = DEBUG_VERBOSE`
- 第一个非选项参数 → `upstream_ip`
- 第二个非选项参数 → `table_file`
- 默认：上游 `202.106.0.20`，文件 `dnsrelay.txt`

### init_server() — 服务器初始化

```c
static int init_server(config_t *cfg)
```

1. `socket(AF_INET, SOCK_DGRAM, 0)` — 创建 UDP socket
2. 调用 `ioctlsocket()` 或 `fcntl()` 设**非阻塞模式**（select 配合非阻塞 socket）
3. `setsockopt(SO_REUSEADDR)` — 允许地址重用（快速重启）
4. `bind()` 到 `0.0.0.0:53`（所有网卡的 53 端口）

### 事件循环 — select() 多路复用

```c
while (1) {
    FD_ZERO(&read_fds);
    FD_SET(cfg.server_sock, &read_fds);     // 监听客户端
    FD_SET(upstream_sock, &read_fds);       // 监听上游响应

    tv.tv_sec = 1;  // 1 秒超时
    ret = select(max_fd, &read_fds, NULL, NULL, &tv);
```

**三个分支**：

| 分支 | 条件 | 动作 |
|------|------|------|
| `ret < 0` | 出错 | 如果 `EINTR` 继续，否则退出 |
| `ret == 0` | 超时 | 调 `relay_check_timeouts()` 和 `cache_cleanup()` |
| `ret > 0` | 有数据 | 检查两个 socket 哪个可读 |

**server_sock 可读**（客户端发来查询）：
1. `recvfrom()` 收报文
2. 调用 `handle_client_query()` 处理

**upstream_sock 可读**（上游 DNS 返回响应）：
1. `recvfrom()` 收报文
2. 调用 `handle_upstream_response()` 处理

### handle_client_query() — 处理客户端查询

```c
static void handle_client_query(config_t *cfg, const uint8_t *query, ...)
```

**三级查询流程**：

```
                   客户端查询
                       │
                       ▼
         ┌──────────────────────┐
         │  ① cache_get()      │──命中──→ dns_build_response() + sendto()
         │  查缓存              │          返回缓存结果
         └──────────────────────┘
                       │ 未命中
                       ▼
         ┌──────────────────────┐
         │  ② table_lookup()   │──命中──→ dns_build_response() + sendto()
         │  查本地对照表        │          0.0.0.0 → NXDOMAIN
         └──────────────────────┘          正常 IP → A 记录
                       │ 未命中
                       ▼
         ┌──────────────────────┐
         │  ③ relay_forward()  │──→ 转发给上游 DNS
         │  中继转发            │     select 等响应
         └──────────────────────┘
```

**细节**：
- 每次先调 `dns_extract_question()` 从报文中提取域名和查询类型
- 调 `inet_ntop()` 把客户端二进制 IP 转成 `"127.0.0.1"` 字符串打日志
- **缓存命中**后调 `dns_build_response()` 用缓存中的 IP 构造响应，TTL 用 60 秒（因为我们自己缓存的时间不长）
- **本地表命中**后也调 `dns_build_response()`，但 TTL 用 3600 秒（静态表的数据一般较稳定）
- **中继**时调 `relay_forward()` 什么都不用管了，等 select 下一次轮询处理上游响应

### handle_upstream_response() — 处理上游响应

```c
static void handle_upstream_response(config_t *cfg,
                                      const uint8_t *resp, size_t resp_len)
```

1. 检查确实是响应报文（`DNS_GET_QR(hdr) == 1`）
2. 调 `relay_process_response()` — 查 ID 映射表、恢复原始 ID、发回客户端
3. 调 `dns_extract_a_record()` — 从响应中提取 A 记录
4. 如果提取成功（返回 1），调 `cache_put()` 缓存结果供后续查询加速

---

## 函数调用关系总图

```
main()
  ├── socket_init()                     [platform.h]
  ├── parse_args()                      [main.c 内部]
  ├── table_init()                      [table.c]
  ├── table_load_file()                 [table.c]
  │     └── hash_str()                  [table.c 内部]
  ├── init_server()                     [main.c 内部]
  ├── cache_init()                      [cache.c]
  ├── relay_init()                      [relay.c]
  │
  └── select 事件循环
        ├── [server_sock 可读]
        │     └── handle_client_query()
        │           ├── dns_extract_question()     [dns.c]
        │           │     └── dns_decode_name()     [dns.c]
        │           ├── cache_get()                [cache.c]
        │           │     └── cache_hash()          [cache.c 内部]
        │           ├── dns_build_response()        [dns.c]
        │           │     └── dns_skip_name()       [dns.c]
        │           ├── table_lookup()              [table.c]
        │           │     └── hash_str()            [table.c 内部]
        │           └── relay_forward()             [relay.c]
        │                 ├── dns_build_relay_query() [dns.c]
        │                 └── dns_extract_question() [dns.c]
        │
        ├── [upstream_sock 可读]
        │     └── handle_upstream_response()
        │           ├── relay_process_response()    [relay.c]
        │           │     ├── dns_restore_id()      [dns.c]
        │           │     └── dns_extract_question() [dns.c]
        │           ├── dns_extract_a_record()      [dns.c]
        │           │     ├── dns_skip_name()        [dns.c]
        │           │     └── dns_decode_name()      [dns.c]
        │           └── cache_put()                 [cache.c]
        │                 └── cache_hash()           [cache.c 内部]
        │
        └── [超时]
              ├── relay_check_timeouts()            [relay.c]
              │     └── dns_extract_question()       [dns.c]
              └── cache_cleanup()                   [cache.c]
```

---

## 编译命令速查

```bash
# 完整编译（gcc，0 警告）
gcc -Wall -Wextra -std=c99 -O2 -I src -o dnsrelay.exe \
    src/main.c src/dns.c src/table.c src/relay.c src/cache.c \
    -lws2_32

# 运行测试
.\dnsrelay -d 10.3.9.5 dnsrelay.txt
nslookup www.bupt.cn 127.0.0.1
```
