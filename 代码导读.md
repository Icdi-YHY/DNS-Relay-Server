# DNS 中继服务器 — 代码导读与编写详解

> **上篇"代码导读"**：每个模块/函数**做什么**，函数签名、数据结构说明。
> **下篇"编写详解"**：**为什么这么写**，设计决策、边界情况、C 语言技巧、踩坑记录。
>
> 两部分按模块合并，每个模块先讲做什么，再讲怎么写。

---

## 目录

1. [总纲：架构先于代码](#1-总纲架构先于代码)
2. [Makefile — 构建系统](#2-makefile--构建系统)
3. [src/platform.h / platform.c — 平台抽象层](#3-srcplatformh--platformc--平台抽象层)
4. [src/debug.h / debug.c — 调试输出](#4-srcdebugh--debugc--调试输出)
5. [src/util.h / util.c — 工具函数](#5-srcutilh--utilc--工具函数)
6. [src/dns_message.h — DNS 协议结构体定义](#6-srcdns_messageh--dns-协议结构体定义)
7. [src/dns_message.c — DNS 报文编解码实现](#7-srcdns_messagec--dns-报文编解码实现)
8. [src/dns_table.h / dns_table.c — 域名-IP 对照表](#8-srcdnstableh--dnstablec--域名-ip-对照表)
9. [src/dns_cache.h / dns_cache.c — 动态缓存](#9-srcdns_cacheh--dns_cachec--动态缓存)
10. [src/id_map.h / id_map.c — ID 转换表](#10-srcid_maph--id_mapc--id-转换表)
11. [src/dns_relay.h / dns_relay.c — 中继转发模块](#11-srcdns_relayh--dns_relayc--中继转发模块)
12. [src/main.c — 主程序](#12-srcmainc--主程序)
13. [模块调用关系图](#13-模块调用关系图)
14. [附录：编码原则总结](#14-附录编码原则总结)

---

## 1. 总纲：架构先于代码

### 做什么

本程序是北京邮电大学《计算机网络课程设计》的 DNS 中继服务器实现。位于客户端与外部 DNS 服务器之间，提供三级查询处理：

```
客户端 → ① 缓存查询 → ② 本地对照表 → ③ 上游DNS中继
               │               │               │
               ▼               ▼               ▼
           命中返回         命中返回          解析返回
```

三种核心功能：

| 功能 | 说明 | 对应场景 |
|------|------|---------|
| **DNS 服务器** | 从本地"域名-IP"对照表查找，直接返回结果 | 常见网站本地加速 |
| **不良网站拦截** | 表中 IP 为 `0.0.0.0` 时返回 NXDOMAIN | 广告/恶意域名屏蔽 |
| **DNS 中继** | 未命中时转发到上游 DNS 并返回结果 | 正常上网浏览 |

程序由 **9 个 .c 文件 + 对应的 .h 文件** 组成，每个模块只做一件事。

### 怎么写 —— 模块划分三问

写代码前先问自己：

| 问题 | 答案 |
|------|------|
| **哪些逻辑是互不依赖的？** | DNS 编解码、对照表查找、缓存管理、ID 映射 —— 各自独立，拆成独立模块 |
| **哪些逻辑是平台相关的？** | Socket 创建、初始化、关闭 —— 全抽到 platform 层 |
| **主程序承担什么角色？** | 持有所有模块的实例，编排执行流程，不做具体业务 |

### 怎么写 —— 为什么用 1 个 socket 而不是 2 个？

| 方案 | 优点 | 缺点 |
|------|------|------|
| 1 个 socket | 代码简洁，只 select 1 个 fd，通过源地址区分来源 | 逻辑上需要区分客户端 vs 上游 |
| 2 个 socket | 逻辑分离清晰，不用判源地址 | 需要 select 2 个 fd，代码更复杂 |

**本项目选择 1-socket**，理由：UDP 是无连接的，一个 socket 可以同时与多个地址通信。收到数据报时比较源地址——等于上游 DNS 地址就是上游响应，否则是客户端查询。只需要多一个 `if` 判断，但省掉了整个第二个 socket 的管理代码。

---

## 2. Makefile — 构建系统

### 做什么

**文件位置**：`Makefile`

定义编译规则，支持 Windows（MinGW/MSVC）和 Linux。

```
CC = gcc
CFLAGS = -Wall -Wextra -std=c99 -O2
SRC = src/main.c src/platform.c src/debug.c src/util.c
      src/dns_message.c src/dns_table.c src/dns_cache.c
      src/id_map.c src/dns_relay.c
LDFLAGS = -lws2_32    # 仅 Windows
```

| 命令 | 作用 |
|------|------|
| `make` | 编译整个项目 |
| `make clean` | 删除中间文件 |

### 怎么写

**如何一步步写出 Makefile：**

```
第 1 步：先能手动编译
gcc -Wall -Wextra -std=c99 -O2 -I src -o dnsrelay.exe src/main.c -lws2_32

第 2 步：把命令写成 Makefile 变量
CC = gcc
CFLAGS = -Wall -Wextra -std=c99 -O2
LDFLAGS = -lws2_32

第 3 步：每新增一个 .c 文件就加进 SRC
SRC = src/main.c src/dns_message.c ...  # 逐次追加

第 4 步：加平台判断（Windows 才要 -lws2_32）
ifeq ($(OS),Windows_NT)
    LDFLAGS += -lws2_32
endif
```

**要点**：
- Windows 额外链接 `ws2_32`（Winsock 库）
- Linux 不需要额外链接
- `-I src` 让编译器找 `#include "xxx.h"` 时去 `src/` 目录

---

## 3. src/platform.h / platform.c — 平台抽象层

### 做什么

把 Windows 和 Linux 的 socket 编程差异集中在这里，其他代码调用统一接口。

**抽象接口**：

| 接口 | Windows 实现 | Linux 实现 |
|------|-------------|-----------|
| `socket_init()` | 调用 `WSAStartup()` | 空操作，返回 0 |
| `socket_cleanup()` | 调用 `WSACleanup()` | 空操作 |
| `close_socket(s)` | `closesocket(s)` | `close(s)` |
| `socket_errno()` | `WSAGetLastError()` | `errno` |
| `socket_t` | `typedef SOCKET socket_t;` | 同左 |

**DNS 协议常量**：

```c
#define DNS_PORT            53
#define MAX_DNS_PACKET      512
#define DNS_DEFAULT_TIMEOUT_MS 3000
```

### 怎么写

**编写顺序**：

```
第 1 步：用 static inline 把所有函数放头文件里
   → 简单，不需要 .c 文件
   
第 2 步：重构时再拆出 platform.c
   → 函数体放 .c，头文件只留声明
   → 好处：函数只生成一份代码，不是每个编译单元一份
```

**设计决策详解**：

**为什么用宏而不是函数封装 `close_socket`？**

```c
// 宏：调用处直接展开，没有函数调用开销
#define close_socket(s)  closesocket(s)

// 如果用函数：
void close_socket(SOCKET s) { closesocket(s); }
// 问题：多一次函数调用，且 SOCKET 在 Windows 和 Linux 类型不同，
// 用宏可以避免类型不匹配
```

**`SOCKET` 类型为什么要抽象？** Windows 的 `SOCKET` 是 `typedef UINT_PTR`，本质是个指针大小的整数，不是 `int`。Linux 下 socket 描述符是 `int`。所以需要：

```c
// Windows 自带 SOCKET 类型
// Linux 需要：
typedef int SOCKET;
#define INVALID_SOCKET (-1)
```

**`socket_init()` 为什么放 `.c` 文件？** `static inline` 在头文件中，每 `#include` 一次就生成一份代码。放 `.c` 文件只编译一次，链接时只留一份。

---

## 4. src/debug.h / debug.c — 调试输出

### 做什么

提供分级调试输出，通过 `-d` / `-dd` 命令行参数控制详细程度。

**全局变量**：

```c
extern int g_debug_level;    // 0=无, 1=基本(-d), 2=详细(-dd)
extern int g_query_seq;      // 递增查询序号
```

**函数**：

| 函数 | 说明 |
|------|------|
| `debug_timestamp()` | 返回 `"HH:MM:SS"` 格式时间串 |
| `dump_hex(fp, data, len)` | 十六进制转储报文内容（`-dd` 级别使用） |

**输出宏**：

```c
#define DEBUG(level, fmt, ...)    // 按级别输出 [时间戳] [序号] 消息
#define DEBUG_ERROR(fmt, ...)     // 无条件输出错误
```

输出格式：`[10:51:20] [1] 查询: www.baidu.com  类型= 1  客户端=127.0.0.1`

### 怎么写

**编写顺序**：

```
第 1 步：先写最小版本——就一个 printf：
   if (g_debug_level >= 1) printf(...);
   
第 2 步：改成宏——方便控制"不调试时零开销"：
   #define DEBUG(level, fmt, ...) \
       do { if (g_debug_level >= (level)) fprintf(stderr, ...); } while(0)

第 3 步：加时间戳 + 序号 + .c 文件
```

**设计决策详解**：

**为什么用宏而不是函数？**

```c
// 宏版本：如果 g_debug_level = 0，编译器能看到 if(0) 里面的代码
// 根本不会执行，甚至不会生成函数调用
#define DEBUG(level, fmt, ...)                     \
    do {                                            \
        if (g_debug_level >= (level))               \
            fprintf(stderr, fmt "\n", ##__VA_ARGS__);\
    } while (0)

// 函数版本：即使不输出，也要传参、调用，有固定开销
void debug(int level, const char *fmt, ...);
```

**`do { } while(0)` 为什么必须加？**

```c
// 如果不加，下面这种代码会 break：
if (cond)
    DEBUG(1, "hello");   // 展开后：if(cond) do { ... } while(0); ← 这里没问题
else
    do_something();      // else 悬空到 while(0) 的分号，编译错误！
```

加上 `do { } while(0)` 后，宏展开成一个**语句**（带分号），不是块，不会导致 else 悬空。

**`##__VA_ARGS__` 是什么？** GNU 扩展，允许可变参数为空。

```c
DEBUG(1, "hello");           // 展开：fprintf(stderr, "hello" "\n");
// 没有 ##，上面的展开会多一个逗号：fprintf(stderr, "hello" "\n", );
// ## 的作用是：如果可变参数为空，删掉前面的逗号
```

**为什么 `g_debug_level` 用 `int` 而不是 `enum`？** `-d` 和 `-dd` 是命令行参数，用 `int` 可以直接和 1、2 比较。如果用 `enum`，比较时也要转 int，多此一举。

---

## 5. src/util.h / util.c — 工具函数

### 做什么

提供域名编码/解码、IP 地址转换等通用工具函数。

| 函数 | 说明 |
|------|------|
| `encode_domain_name(buf, domain)` | 将 "www.bupt.cn" 编码为 DNS 长度前缀格式 |
| `decode_domain_name(msg, len, offset, out)` | 解码 DNS 域名，支持压缩指针 |
| `ip_str_to_int(ip_str)` | 将 "192.168.1.1" 转为网络字节序整数 |
| `ip_int_to_str(ip, buf)` | 将 IP 整数转为点分字符串 |

### 怎么写

**设计决策**：为什么不直接用 `inet_pton` / `inet_ntop`，而要包装一层？

```c
// 直接调用的坏处：
struct in_addr addr;
inet_pton(AF_INET, "192.168.1.1", &addr);
// 返回值还要检查 != 1，调用处代码多了 3 行

// 封装后的好处：
uint32_t ip = ip_str_to_int("192.168.1.1");
if (ip == 0) { /* 出错处理 */ }
// 一行搞定，调用处更干净
```

**实现很简单**，本质就是一行封装：

```c
uint32_t ip_str_to_int(const char *ip_str) {
    struct in_addr addr;
    return inet_pton(AF_INET, ip_str, &addr) == 1 ? addr.s_addr : 0;
}
```

这个模块是**最后写的**，不会影响程序功能，只是让其他地方的代码更整洁。

---

## 6. src/dns_message.h — DNS 协议结构体定义

### 做什么

定义 DNS 报文的数据结构（严格按 RFC 1035 §4.1），声明所有 DNS 编解码函数。

### DNSHeader —— DNS 头部结构体（12 字节）

```c
typedef struct PACKED {
    uint16_t id;         // Transaction ID
    uint16_t flags;      // QR|OPCODE|AA|TC|RD|RA|Z|RCODE
    uint16_t qdcount;    // Question 数
    uint16_t ancount;    // Answer 数
    uint16_t nscount;    // Authority 数
    uint16_t arcount;    // Additional 数
} DNSHeader;
```

### DNSQuestion —— 解析后的查询信息

```c
typedef struct {
    char     qname[256];   // 解码后的域名
    uint16_t qtype;        // 查询类型 (A=1, AAAA=28, ...)
    uint16_t qclass;       // 查询类别 (IN=1)
} DNSQuestion;
```

### Flags 位域操作宏

| 宏 | 位 | 作用 |
|----|----|------|
| `DNS_GET_QR(h)` | 15 | 取 QR：0=查询，1=响应 |
| `DNS_GET_RCODE(h)` | 3-0 | 响应码 |
| `DNS_SET_RCODE(h, v)` | 3-0 | 设响应码 |

响应码常量：`DNS_RCODE_NOERROR = 0`，`DNS_RCODE_NXDOMAIN = 3`

### DNSResourceRecord —— 资源记录结构体

```c
typedef struct PACKED {
    uint16_t type;      // A/AAAA/MX/CNAME...
    uint16_t cls;       // 通常为 IN(1)
    uint32_t ttl;       // 生存时间（秒）
    uint16_t rdlength;  // RDATA 的长度
    // uint8_t rdata[rdlength]; 紧随其后
} DNSResourceRecord;
```

### 函数声明

| 函数 | 一句话说明 |
|------|----------|
| `dns_decode_query()` | 解码请求报文，提取 Header 和 Question |
| `dns_encode_name()` | 把域名编码成 DNS 长度前缀格式 |
| `dns_decode_name()` | 把 DNS 格式解码回点分字符串，支持压缩指针 |
| `dns_build_response()` | 根据 Header+Question 构造响应报文（成功或 NXDOMAIN） |
| `dns_build_relay_query()` | 构造转发报文（替换 ID） |
| `dns_restore_id()` | 把响应 ID 改回客户端的原始 ID |
| `dns_extract_a_record()` | 从上游响应中提取第一条 A 记录的 IP 地址 |

### 怎么写

**编写顺序**：

```
第 1 步：先定义结构体 DNSHeader、DNSQuestion
   → 打开 RFC 1035 §4.1，对着协议图写字段
   
第 2 步：定义常量宏（QTYPE, RCODE 等）
   → 一边写代码一边查 RFC，不用一次性背完

第 3 步：声明函数原型
   → 写一个声明一个，不用一次写完所有函数
```

**设计决策详解**：

**`#pragma pack(1)` 为什么是必须的？**

```c
// 不加 pack 时，struct 可能有填充字节：
struct { uint16_t a; uint16_t b; uint16_t c; uint16_t d;
         uint16_t e; uint16_t f; };
// sizeof = 12 ✅ 6 个 uint16_t 正好 12

// 但如果有 uint8_t 和 uint16_t 混排：
struct { uint16_t a; uint8_t b; uint16_t c; };
// 默认对齐：a 在 0-1, b 在 2, 3 填充, c 在 4-5 → sizeof = 6
// pack(1)：a 在 0-1, b 在 2, c 在 3-4 → sizeof = 5

// DNS 报文是逐字节的，没有填充，所以必须 pack(1)
```

**为什么不用位域（bit field）表示 flags？**

```c
// 位域的字节序和内存布局是 implementation-defined：
struct {
    unsigned int QR : 1;
    unsigned int OPCODE : 4;
    // ...
};
// 在小端 x86 和大端 ARM 上，哪个 bit 对应哪个字段可能不同

// 用宏+位运算，100% 可移植：
#define DNS_GET_QR(h)    (((h)->flags & 0x8000) >> 15)
#define DNS_GET_RCODE(h) ((h)->flags & 0x000F)
```

**为什么 `DNSQuestion` 单独成一个结构体而不是直接读字段？**

把"解析过程"和"解析结果"分开——`dns_decode_query` 负责解析，`DNSQuestion` 存解析结果。后续函数（`dns_build_response`）只关心结果，不需要知道报文格式。

---

## 7. src/dns_message.c — DNS 报文编解码实现

### 做什么

实现 `dns_message.h` 中声明的所有函数，核心是域名编码/解码和报文构造。

### dns_decode_query() —— 解码查询报文

```c
int dns_decode_query(const uint8_t *raw, int len,
                     DNSHeader *hdr, DNSQuestion *q)
```

**流程**：
1. 检查报文长度 ≥ 12 字节（`sizeof(DNSHeader)`）
2. 复制头部到 `hdr`
3. 跳过头部到 Question 段
4. 调用 `dns_decode_name()` 解码域名
5. 读 2 字节 QTYPE + 2 字节 QCLASS

### dns_encode_name() —— 域名编码

```c
int dns_encode_name(uint8_t *buf, const char *name);
```

把 `"www.bupt.cn"` 编码成 `\x03www\x05bupt\x03edu\x02cn\x00`。

**编码规则**：每个标签前加长度字节，末尾加 `\x00`，标签长度 ≤ 63。

### dns_decode_name() —— 域名解码（支持压缩指针）

```c
const char *dns_decode_name(const uint8_t *raw, int rawlen,
                            int *offset, char *out);
```

**关键特性 —— 压缩指针**：DNS 为了节省空间，同一个域名第二次出现时用指针指向前面的位置。如果字节最高两位是 `11`（`>= 0xC0`），和后一个字节拼成偏移量。

### dns_build_response() —— 构建响应报文

```c
int dns_build_response(const DNSHeader *req_hdr, const DNSQuestion *q,
                       uint32_t ip, int nxdomain, uint8_t *out)
```

| 参数 | 含义 |
|------|------|
| `nxdomain = 1` | 返回 NXDOMAIN（RCODE=3），拦截 |
| `nxdomain = 0, ip ≠ 0` | 返回 A 记录，本地解析 |

**构建过程**：
1. 写头部：QR=1, RA=1，继承 RD；nxdomain 时 RCODE=3
2. 写 Question 段
3. 如果 nxdomain，到此为止（无 Answer 段）
4. 添加 Answer 段：NAME 用压缩指针指向 Question 中的域名

### dns_build_relay_query() —— 构建转发报文

```c
int dns_build_relay_query(const DNSHeader *orig_hdr, const DNSQuestion *q,
                          uint16_t new_id, uint8_t *out)
```

复制头部（改 ID）+ 编码域名 + QTYPE/QCLASS。

### dns_restore_id() / dns_extract_a_record()

- `dns_restore_id()` — 把上游响应中的 ID 恢复为原始客户端 ID
- `dns_extract_a_record()` — 提取第一条 A 记录的 IP 和 TTL

### 怎么写

**编写顺序**：

```
第 1 步：写 dns_encode_name 和 dns_decode_name
   → 这是最底层、最核心的两个函数
   → 先单独测试：编码 "www.bupt.cn"，解码回来，验证相等

第 2 步：写 dns_decode_query
   → 依赖 dns_decode_name
   → 测试：用 nslookup 发一个真实查询，抓包，用代码解析

第 3 步：写 dns_build_response
   → 依赖 dns_encode_name
   → 测试：构造响应发回客户端，nslookup 能收到 IP

第 4 步：写 dns_build_relay_query、dns_restore_id、dns_extract_a_record
   → 这些是辅助函数，第 3 步之后再写
```

**设计决策详解**：

**`dns_decode_name` 的压缩指针逻辑逐行拆解**：

```c
while (pos < rawlen && raw[pos] != 0) {
    if ((raw[pos] & 0xC0) == 0xC0) {
        // 0xC0 = 1100 0000
        // 最高两位是 11 → 压缩指针
        // 取低 6 位 + 下一个字节的 8 位 = 14 位偏移
        int ptr = ((raw[pos] & 0x3F) << 8) | raw[pos + 1];
        // 从报文偏移 ptr 处读域名
        // 但先记下"跳过指针后的位置"（用于返回值）
        if (!jumped) { *offset = pos + 2; jumped = 1; }
        pos = ptr;
        continue;  // 跳转后继续读
    }
    // 普通标签：读长度字节，再读内容
    uint8_t len = raw[pos++];
    memcpy(out + out_pos, raw + pos, len);
    out_pos += len;
    pos += len;
}
```

**压缩指针为什么难**：因为一个域名可能在报文里被多次引用，每次都用指针指回第一次出现的位置。`dns_decode_name` 需要支持"跳转后继续读常规标签"——`jumped` 标志位解决了这个问题。

**`dns_build_response` 的 `nxdomain` 参数为什么比 `ip_addr==0` 好？**

原来的写法：`ip_addr == 0` 隐含 NXDOMAIN。但 `0.0.0.0` 在拦截功能中正好就是 `ip_addr == 0`，语义混淆。

新写法：显式的 `nxdomain` 标志位。

```c
// 原来的（语义不清晰）：
dns_build_response(query, len, resp, sizeof(resp), 0, 0);
// 0 到底是表示 NXDOMAIN 还是表示 IP 地址 0.0.0.0？

// 新的（语义清晰）：
dns_build_response(&hdr, &q, 0, 1, resp);  // nxdomain=1 → 拦截
dns_build_response(&hdr, &q, ip, 0, resp);  // nxdomain=0 → 返回 IP
```

---

## 8. src/dns_table.h / dns_table.c — 域名-IP 对照表

### 做什么

读入"域名-IP 地址"对照表文件，构建内存哈希表，提供 O(1) 查找。

### 数据结构

```c
typedef struct TableEntry {
    char   domain[256];
    uint32_t ip;              // 0 表示拦截
    struct TableEntry *next;
} TableEntry;

typedef struct {
    TableEntry **buckets;     // 动态分配的哈希桶数组
    int         size;
    int         count;
} DNSTable;
```

### 五个函数

| 函数 | 作用 |
|------|------|
| `dns_table_init(&t, size)` | 初始化哈希表，分配 size 个桶 |
| `dns_table_load(&t, filename)` | 从文件读入域名-IP 对，构造哈希表 |
| `dns_table_lookup(&t, domain, &ip)` | 查域名，返回 1/0 |
| `dns_table_add(&t, domain, ip, ttl)` | 动态添加条目 |
| `dns_table_destroy(&t)` | 释放所有内存 |

### 怎么写

**编写顺序**：

```
第 1 步：实现 hash_string 哈希函数（10 行）
第 2 步：实现 dns_table_init + dns_table_destroy（20 行）
第 3 步：实现 dns_table_lookup（15 行）
第 4 步：实现 dns_table_load（50 行）
   → 单独测试：加载 dnsrelay.txt，查 www.bupt.cn 看结果
第 5 步：实现 dns_table_add（15 行）
```

**设计决策详解**：

**DJB2 哈希算法**：

```c
static unsigned long hash_string(const char *str) {
    unsigned long hash = 5381;
    int c;
    while ((c = *str++))
        hash = ((hash << 5) + hash) + c;  // hash * 33 + c
    return hash;
}
```

**为什么是 5381 和 33？**

- 33 被 Daniel J. Bernstein 实验验证对字符串有很好的分布性
- `hash << 5 + hash` 等于 `hash * 33`——移位+加法比乘法指令快
- 5381 是"魔法数字"，和 33 配合能让哈希值在 32 位空间均匀散布。你不需要背它，记住 DJB2 这个算法名就行

**为什么用链地址法而不是开放地址法？**

| 方案 | 优点 | 缺点 |
|------|------|------|
| 链地址法（链表） | 实现简单，删除容易，碰撞时退化平缓 | 多存一个指针开销 |
| 开放地址法（ probing） | 内存紧凑，缓存友好 | 删除复杂（要打墓碑标记），碰撞时雪崩 |

**本项目选链地址法**，因为实现简单，删除容易，且域名数量可控。

**为什么 `domain` 用 `char[256]` 定长数组？**

```c
// 方案 A：定长数组
char domain[256];  // 域名最长 255 字节 + 1 个 \0

// 方案 B：动态分配
char *domain;      // 要多一次 malloc + free
```

选 A：省一次 malloc/free，结构体大小可控（每个条目 256+4+8=268 字节，还好）。

**为什么头插法？**

```c
// 头插法：新记录插在链表头部
entry->next = buckets[idx];
buckets[idx] = entry;

// 尾插法：要遍历到末尾或维护 tail 指针
if (buckets[idx] == NULL) {
    buckets[idx] = entry;
} else {
    TableEntry *tail = buckets[idx];
    while (tail->next) tail = tail->next;
    tail->next = entry;
}
```

头插法 2 行代码，尾插法至少 8 行。而且时间局部性——最近插入的记录最可能被查，头插法恰好把它放在了链表最前面。

**`dns_table_load` 的逐行解析**：

```c
while (fgets(line, sizeof(line), fp)) {
    // 第 1 步：跳过前导空白
    while (*p && isspace(*p)) p++;
    // 第 2 步：跳过空行和 # 注释
    if (*p == '\0' || *p == '\n' || *p == '#') continue;
    // 第 3 步：读 IP（直到遇到空白）
    // 第 4 步：读域名（直到遇到空白或换行）
    // 第 5 步：inet_pton 转二进制
    // 第 6 步：哈希 + 头插法插入
}
```

**为什么要跳过空白、注释、空行？** 对照表文件是人手写的，格式不可能完全规范。如果有人加了空格缩进、空行分隔、注释说明，程序不应该崩溃。

---

## 9. src/dns_cache.h / dns_cache.c — 动态缓存

### 做什么

缓存从外部 DNS 服务器查询到的**完整响应报文**，支持 TTL 过期自动清理。

### 数据结构

```c
typedef struct CacheEntry {
    char   domain[256];
    uint8_t *response;         // 缓存的完整响应报文
    int     response_len;
    time_t  expiry_time;
    struct CacheEntry *next;
} CacheEntry;

typedef struct {
    CacheEntry **buckets;
    int         size;
    int         count;
} DNSCache;
```

### 五个函数

| 函数 | 作用 |
|------|------|
| `dns_cache_init(&c, size)` | 初始化缓存 |
| `dns_cache_put(&c, domain, response, len, ttl)` | 存入完整响应报文 |
| `dns_cache_get(&c, domain)` | 返回 `CacheEntry*`，未命中返回 NULL |
| `dns_cache_evict_expired(&c)` | 清理所有过期条目 |
| `dns_cache_destroy(&c)` | 释放所有内存 |

### 怎么写

**编写顺序**：

```
第 1 步：实现 dns_cache_init + dns_cache_destroy
第 2 步：实现 dns_cache_put（存完整报文）
第 3 步：实现 dns_cache_get
第 4 步：实现 dns_cache_evict_expired
```

**设计决策详解**：

**为什么存完整报文而不是只存 IP？**

```c
// 方案 A：只存 IP
cache_put(domain, ip, ttl);
// 命中后：还要重新构造响应报文
dns_build_response(&hdr, &q, ip, 0, resp);
sendto(sock, resp, len, 0, &client, sizeof(client));

// 方案 B：存完整报文
cache_put(domain, response, len, ttl);
// 命中后：直接 sendto 回去
sendto(sock, ce->response, ce->response_len, 0, &client, sizeof(client));
```

方案 B 缓存命中时**零构造开销**。代价是多 malloc 几百字节（DNS 报文最多 512 字节），可以接受。

**内存管理**：

```c
int dns_cache_put(DNSCache *c, const char *domain,
                  const uint8_t *response, int len, int ttl) {
    // 新条目要 malloc 两次：一次 CacheEntry，一次 response 报文
    CacheEntry *entry = malloc(sizeof(CacheEntry));
    entry->response = malloc(len);
    memcpy(entry->response, response, len);

    // 更新条目要先 free 旧的 response
    free(e->response);  // 旧报文
    e->response = malloc(len);  // 新报文
    memcpy(e->response, response, len);
}
```

**每次都要 malloc + memcpy，为什么不用指针共享？** 因为上游响应报文是 `main.c` 栈上的 `buffer[]`，不拷贝的话上游响应一覆盖就没了。

**TTL 管理**：

```c
time_t expire = time(NULL) + ttl;
// 最小值 60 秒——避免刚缓存就过期，白缓存了
if (ttl < 60) ttl = 60;
```

**`dns_cache_evict_expired` 为什么用二级指针？**

```c
void dns_cache_evict_expired(DNSCache *c) {
    for (int i = 0; i < c->size; i++) {
        CacheEntry **pp = &c->buckets[i];  // 二级指针！
        while (*pp) {
            CacheEntry *e = *pp;
            if (now >= e->expiry_time) {
                *pp = e->next;  // 跳过当前节点
                free(e->response);
                free(e);
            } else {
                pp = &e->next;  // 移到下一个
            }
        }
    }
}
```

**为什么用二级指针？** 单向链表删除节点时，需要修改**前一个节点**的 `next` 指针。一级指针遍历的话要维护 `prev` 变量：

```c
// 一级指针方案：要维护 prev
CacheEntry *prev = NULL, *e = buckets[i];
while (e) {
    if (expired) {
        if (prev) prev->next = e->next;
        else buckets[i] = e->next;  // 删除头节点要特殊处理
        free(e);
    }
    prev = e;
    e = e->next;
}

// 二级指针方案：不需要 prev，*pp 永远是"指向当前节点的指针"
// 删除头节点和删除中间节点用同一份代码
```

二级指针是 Linux 内核链表遍历的常见技巧，**删除头节点和删除中间节点用同一段代码**，不需要 if-else。

---

## 10. src/id_map.h / id_map.c — ID 转换表

### 做什么

管理客户端原始 Transaction ID 与转发 ID 的映射关系，支持多客户端并发查询。

### 设计思想

当多个客户端同时查询时，各自携带一个 16 位 ID。直接转发到上游 DNS 会导致 ID 冲突（两个客户端用了同一个 ID，上游返回时不知道给谁）。解决方法是**替换 ID**：

```
客户端 A：ID=0x1234  →  中继分配 relay_id=0x0001  →  转发 (ID=0x0001)
客户端 B：ID=0x1234  →  中继分配 relay_id=0x0002  →  转发 (ID=0x0002)
上游返回 ID=0x0001 → 中继查表 → 恢复 ID=0x1234 → 发给客户端 A
```

### 数据结构

```c
typedef struct {
    uint16_t    orig_id;
    uint16_t    relay_id;
    struct sockaddr_in client_addr;
    time_t      send_time;
    int         in_use;
} IDMapEntry;

typedef struct {
    IDMapEntry *entries;
    int         capacity;
    int         count;
    uint16_t    next_relay_id;
} IDMap;
```

### 五个函数

| 函数 | 说明 |
|------|------|
| `id_map_init(&m, capacity)` | 初始化 ID 表 |
| `id_map_alloc(&m, orig_id, client, &relay_id)` | 分配转发 ID，记录映射 |
| `id_map_lookup(&m, relay_id, &orig_id, &client)` | 根据转发 ID 查找原始信息 |
| `id_map_free(&m, relay_id)` | 释放 ID 映射项 |
| `id_map_cleanup(&m, timeout)` | 清理超时的映射项 |

### 怎么写

**编写顺序**：

```
第 1 步：实现 id_map_init + id_map_destroy
第 2 步：实现 id_map_alloc（最核心）
第 3 步：实现 id_map_lookup + id_map_free
第 4 步：实现 id_map_cleanup
```

**设计决策详解**：

**`id_map_alloc` 的冲突处理**：

```c
uint16_t new_id;
do {
    conflict = 0;
    new_id = m->next_relay_id++;       // 递增
    if (m->next_relay_id == 0)          // 绕回
        m->next_relay_id = 1;          // 跳过 0

    for (int i = 0; i < m->capacity; i++)
        if (m->entries[i].in_use && m->entries[i].relay_id == new_id)
            conflict = 1;
} while (conflict);
```

**为什么有 do-while 冲突检查？** `next_relay_id` 是 16 位无符号整数，最大 65535。如果程序运行时间很长，`next_relay_id` 会绕回。绕回后可能和还在 pending 中的 ID 冲突。

**为什么不用 0 作为 relay_id？** 代码里用 0 表示"无效"（函数返回 -1 表示失败），所以 `next_relay_id` 从 1 开始，绕回时也设为 1。

**超时用 `difftime()` 而不是直接比较为什么？**

```c
// 标准写法：
if (difftime(now, m->entries[i].send_time) >= (double)timeout)

// 为什么不用 now - send_time？
// time_t 可能是无符号整数，now - send_time 如果 send_time 更大
// 会得到很大的正数（无符号溢出），而不是负数
// difftime 保证返回正确的 double 差值
```

---

## 11. src/dns_relay.h / dns_relay.c — 中继转发模块

### 做什么

当本地对照表未命中时，将查询转发给外部 DNS 服务器。ID 映射委托给 `id_map` 模块。

### 关键接口

| 函数 | 说明 |
|------|------|
| `relay_forward(sock, dns_server, query, qlen, client_addr, map)` | 修改 ID 后转发给外部 DNS |

### relay_forward() 流程

```c
int relay_forward(socket_t sock, struct sockaddr_in *dns_server,
                  const uint8_t *query, int qlen,
                  struct sockaddr_in *client_addr, IDMap *map) {
    // 1. 解析查询
    dns_decode_query(query, qlen, &hdr, &q);
    // 2. 分配 relay_id
    id_map_alloc(map, orig_id, client_addr, &relay_id);
    // 3. 构造转发报文
    dns_build_relay_query(&hdr, &q, relay_id, relay_buf);
    // 4. 发送到上游
    sendto(sock, relay_buf, relay_len, 0, dns_server, sizeof(*dns_server));
}
```

### 怎么写

**为什么 relay 模块这么薄？**

在 2-socket 架构中，relay 模块负责：
- 创建/管理上游 socket
- 管理 ID 映射表
- 转发查询
- 处理响应
- 超时检查

在 1-socket 架构中：
- socket 由 main.c 统一管理
- ID 映射由 `id_map` 独立模块管理
- 响应处理和缓存由 main.c 直接处理

所以 relay 只需要做一件事：**转发查询**。

**回滚逻辑**：

```c
if (dns_build_relay_query(...) < 0) {
    id_map_free(map, relay_id);  // 构造失败，释放已分配的 ID
    return -1;
}
```

如果 `dns_build_relay_query` 或 `sendto` 失败，已经分配的 relay_id 要释放掉，否则会造成 ID 泄漏（pending 表慢慢被死条目占满）。

---

## 12. src/main.c — 主程序

### 做什么

程序入口，把以上所有模块串起来。采用 **1-socket 架构**。

### 包含的头文件

```c
#include "platform.h"      // 平台抽象
#include "dns_message.h"   // DNS 报文编解码
#include "dns_table.h"     // 对照表
#include "dns_cache.h"     // 动态缓存
#include "dns_relay.h"     // 中继转发
#include "id_map.h"        // ID 映射
#include "debug.h"         // 调试输出
#include "util.h"          // 工具函数
```

### GlobalState —— 配置结构体

```c
typedef struct {
    char        dns_server[64];
    int         dns_port;
    char        table_path[256];
    socket_t    sock;                // 唯一 socket
    struct sockaddr_in server_addr;
    struct sockaddr_in dns_addr;     // 上游 DNS 地址
    DNSTable    table;               // 静态对照表
    DNSCache    cache;               // 动态缓存
    IDMap       id_map;              // ID 映射表
} GlobalState;
```

### main() —— 主流程

```
① SetConsoleOutputCP(CP_UTF8)     ← 仅 Windows
② socket_init()                   ← 初始化 Winsock
③ parse_args()                    ← 解析命令行参数
④ dns_table_init() + dns_table_load()  ← 加载对照表
⑤ init_server()                   ← 创建 UDP socket，绑 53 端口
⑥ dns_cache_init()                ← 初始化缓存
⑦ id_map_init()                   ← 初始化 ID 映射表
⑧ 进入 select() 事件循环
```

### 1-socket 事件循环

```c
select(state.sock, 超时=1秒)
    │
    ├── 超时 → id_map_cleanup() + dns_cache_evict_expired()
    │
    ├── 收到客户端请求
    │     ├── dns_decode_query() 解析报文
    │     ├── dns_cache_get()    检查缓存
    │     ├── dns_table_lookup() 检查对照表
    │     │     ├── 0.0.0.0 → NXDOMAIN
    │     │     └── 有效 IP → A 记录
    │     └── 未命中 → relay_forward()
    │
    └── 收到上游响应
          ├── id_map_lookup() 找原始客户端
          ├── dns_restore_id() 恢复原始 ID
          ├── sendto() 发给客户端
          ├── id_map_free() 释放映射
          └── dns_cache_put() 缓存
```

### 区分来源的逻辑

```c
// 收到数据后判断来源：
if (from.sin_addr.s_addr == state.dns_addr.sin_addr.s_addr &&
    from.sin_port == state.dns_addr.sin_port) {
    // 来自上游 DNS 的响应
    handle_upstream_response(...);
} else {
    // 来自客户端的 DNS 查询
    handle_client_query(...);
}
```

### 怎么写

**编写顺序**：

```
第 1 步：写最小 main.c（UDP 收包 + 打印）——验证 socket 通
第 2 步：加 dns_decode_query + 打印域名——验证报文解析
第 3 步：加 dns_build_response 硬编码回复——验证 DNS 响应构造
第 4 步：加 DNS table 加载和查找——替换硬编码
第 5 步：加 id_map + relay_forward——实现中继
第 6 步：加上游响应处理 + 源地址判断——完成 1-socket 转发
第 7 步：加 dns_cache——缓存中继结果
第 8 步：加 debug + util——完善代码质量
```

**设计决策详解**：

**为什么 select 超时设 1 秒？**

因为有两个定时任务需要每 1 秒执行一次：
1. `id_map_cleanup` — 检查超时 5 秒的 ID 映射
2. `dns_cache_evict_expired` — 清理过期缓存

设 1 秒保证了每秒一次的清理粒度，同时不会因为频繁 wakeup 浪费 CPU。

**`g_query_seq` 在哪递增？**

在 `handle_client_query` 的第一行——每收到一个客户端查询，序号加 1。这样日志里的序号和查询一一对应，方便调试时追踪。

**`init_server` 为什么同时构造了 `dns_addr`？**

因为 1-socket 架构需要把上游 DNS 地址存在 `GlobalState` 里，后续每次收到数据都用它来区分来源：

```c
// init_server 里：
struct in_addr addr;
inet_pton(AF_INET, state->dns_server, &addr);
state->dns_addr.sin_addr = addr;
state->dns_addr.sin_port = htons(state->dns_port);

// 事件循环里：
if (from.sin_addr.s_addr == state->dns_addr.sin_addr.s_addr &&
    from.sin_port == state->dns_addr.sin_port)
    // 上游响应
```

---

## 13. 模块调用关系图

```
main.c  select() 事件循环
  │
  ├── parse_args()                   解析命令行参数
  │
  ├── init_server()                  创建 UDP socket + bind 53
  │
  ├── 收到客户端查询:
  │     ├── dns_decode_query()       [dns_message.c]
  │     │     ├── dns_decode_name()  [dns_message.c]
  │     │     └── dns_encode_name()  [dns_message.c]
  │     ├── dns_cache_get()          [dns_cache.c]
  │     │     └── cache_hash()       [dns_cache.c 内部]
  │     ├── dns_table_lookup()       [dns_table.c]
  │     │     └── hash_string()      [dns_table.c 内部]
  │     ├── dns_build_response()     [dns_message.c]
  │     │     └── dns_encode_name()  [dns_message.c]
  │     └── relay_forward()          [dns_relay.c]
  │           ├── dns_decode_query() [dns_message.c]
  │           ├── id_map_alloc()     [id_map.c]
  │           └── dns_build_relay_query() [dns_message.c]
  │
  ├── 收到上游响应:
  │     ├── id_map_lookup()          [id_map.c]
  │     ├── dns_restore_id()         [dns_message.c]
  │     ├── id_map_free()            [id_map.c]
  │     └── dns_cache_put()          [dns_cache.c]
  │           └── cache_hash()       [dns_cache.c 内部]
  │
  └── [超时]
        ├── id_map_cleanup()         [id_map.c]
        └── dns_cache_evict_expired() [dns_cache.c]
```

---

## 14. 附录：编码原则总结

### 原则 1：小即是美

**每个函数不超过 50 行，每个函数只做一件事。**

```c
// 好的例子：
static int parse_args(...);        // 只做参数解析
static int init_server(...);       // 只做 socket 初始化
void handle_client_query(...);     // 只处理客户端查询

// 坏的样子：一个函数 300 行，从参数解析到事件循环到发响应全在里面
```

判断标准：如果函数名里出现了"和"字（比如"解析参数并初始化服务器"），就应该拆成两个函数。

### 原则 2：const 能把 bug 挡在编译期

```c
// 只读参数加 const
int dns_decode_query(const uint8_t *raw, int len, DNSHeader *hdr, DNSQuestion *q);
//                   ^^^^^               ^^^^^
//                   raw 是只读的        len 是值传递

// 输出参数放在前面
void dns_cache_put(DNSCache *c, const char *domain,
                   const uint8_t *response, int len, int ttl);
//                  ^^^^^^^^^^ 输出参数在最前面
```

### 原则 3：显式错误处理

**每一个 `malloc`、`sendto`、`recvfrom` 都检查返回值。**

```c
entry = malloc(sizeof(TableEntry));
if (!entry) {
    DEBUG_ERROR("内存分配失败");
    return -1;
}
```

最隐蔽的 bug 来自"假设不会失败"。UDP sendto 在极端情况下（发送缓冲区满）确实会失败。

### 原则 4：不信任外部输入

所有从网络接收的数据都要做边界检查：

```c
if (!query || qlen < (int)sizeof(DNSHeader)) return -1;  // 长度下限
if (pos + 4 > len) return -1;  // 访问越界检查
if (seg_len > 63) return -1;   // 标签长度上限
```

DNS 报文来自网络，可能被篡改或损坏。**不信任任何来自 recvfrom 的字节**。

### 原则 5：先写注释，再写代码

每个函数前的注释描述了"这个函数做什么、参数是什么、返回什么"，写完注释再写实现。这保证：

1. 写实现时有明确目标
2. 别人（和未来的自己）不用猜这个函数在做什么
3. Doxygen 风格的注释可以直接生成文档

---

## 编译命令速查

```bash
# 完整编译（gcc，0 警告）
gcc -Wall -Wextra -std=c99 -O2 -I src -o dnsrelay.exe \
    src/main.c src/platform.c src/debug.c src/util.c \
    src/dns_message.c src/dns_table.c src/dns_cache.c \
    src/id_map.c src/dns_relay.c \
    -lws2_32

# 运行测试
.\dnsrelay -d 10.3.9.5 dnsrelay.txt
nslookup www.bupt.cn 127.0.0.1
```

---

*文档版本：v2.0 · 2025*

*北京邮电大学 网络工程专业 · 项目式课程阶段2（计算机网络课程设计）*
