# DNS 中继服务器 — 项目文件功能说明

```
C:\Code\DNS-Relay-Server\
│
├── src\                      ← 源代码目录（9 个模块）
│   ├── main.c                ← 程序入口 + 事件循环
│   ├── platform.h            ← 跨平台：Windows/Linux socket 差异
│   ├── platform.c
│   ├── debug.h               ← 调试输出：DEBUG 宏 + 序号 + 时间戳
│   ├── debug.c
│   ├── util.h                ← 工具函数：域名编码/解码、IP 转换
│   ├── util.c
│   ├── dns_message.h         ← DNS 报文编解码（按 RFC 1035）
│   ├── dns_message.c
│   ├── dns_table.h           ← 域名-IP 对照表（哈希表）
│   ├── dns_table.c
│   ├── dns_cache.h           ← 缓存（完整报文，TTL 管理）
│   ├── dns_cache.c
│   ├── id_map.h              ← ID 映射（多客户端并发）
│   ├── id_map.c
│   ├── dns_relay.h           ← 中继转发（发往上游 DNS）
│   └── dns_relay.c
│
├── dnsrelay.exe              ← 编译好的可执行程序
├── dnsrelay.txt              ← 域名-IP 对照表
├── Makefile                  ← 编译
│
├── 操作手册.md                ← 编译、运行、测试
├── 代码导读.md                ← 代码讲解 + 响应报文构造详解
├── 课程设计报告.md             ← 完整实验报告
├── 验收问答准备.md             ← 验收问答 + 代码位置
└── README.md                 ← 项目简介
```

---

## src/main.c — 主控模块

**功能**：程序入口，初始化所有模块，进入 select 事件循环。

| 函数 | 做什么 | 关键行 |
|------|--------|:-----:|
| `main()` | 初始化 socket → 加载对照表 → 创建缓存 → 进入 select 循环 | 55 |
| `parse_args()` | 解析 `-d/-dd/-ttl/IP/文件名` | 203 |
| `init_server()` | 创建 UDP socket，绑定 53 端口 | 246 |
| `handle_client_query()` | **①查缓存 ②查本地表 ③中继** 三级流程 | 305 |
| `handle_upstream_response()` | 收到上游响应 → 还原ID → 发回客户端 → 缓存 | 400 |

**关键设计**：1-socket 架构，通过源地址区分客户端和上游（第 170-181 行）。

---

## src/platform.h / platform.c — 跨平台层

**功能**：隐藏 Windows 和 Linux 的 socket 编程差异。

| 接口 | Windows | Linux |
|------|---------|-------|
| `socket_init()` | `WSAStartup()` | 空操作 |
| `socket_cleanup()` | `WSACleanup()` | 空操作 |
| `close_socket(s)` | `closesocket(s)` | `close(s)` |
| `socket_errno()` | `WSAGetLastError()` | `errno` |
| `SOCKET` 类型 | 系统自带的 `SOCKET` | `typedef int SOCKET` |

**关键设计**：核心代码调统一接口，不出现条件编译。所有差异集中在一个文件。

---

## src/debug.h / debug.c — 调试输出层

**功能**：分级调试输出，带时间戳和递增序号。

| 接口 | 条件 | 说明 |
|------|:----:|------|
| `DEBUG(1, fmt, ...)` | `-d` 参数 | 基本信息：时间、序号、客户端IP、域名 |
| `DEBUG(2, fmt, ...)` | `-dd` 参数 | 详细信息：ID 映射、转发、RCODE |
| `DEBUG_ERROR(fmt, ...)` | 无条件 | 错误信息 |
| `dump_hex()` | — | 十六进制转储报文内容 |

**关键设计**：`setvbuf(stderr, NULL, _IONBF, 0)` 让日志实时显示，不缓冲。

---

## src/util.h / util.c — 工具层

**功能**：域名编码/解码、IP 地址转换的便捷封装。

| 函数 | 做什么 | 底层调用 |
|------|--------|---------|
| `encode_domain_name(buf, name)` | "www.bupt.cn" → `\x03www\x05bupt\x03cn\x00` | `dns_encode_name` |
| `decode_domain_name(msg, len, offset, out)` | DNS 格式 → 点分域名（支持压缩指针） | `dns_decode_name` |
| `ip_str_to_int(str)` | "192.168.1.1" → 32 位整数 | `inet_pton` |
| `ip_int_to_str(ip, buf)` | 32 位整数 → "192.168.1.1" | `inet_ntop` |

---

## src/dns_message.h / dns_message.c — 协议解析层

**功能**：DNS 报文的解析和构建，严格按照 RFC 1035。

**结构体**：

| 结构体 | 对应报文段 | 大小 |
|--------|-----------|:----:|
| `DNSHeader` | 头部 | 12 字节 |
| `DNSQuestion` | 解析后的查询 | — |
| `DNSResourceRecord` | Answer 段的固定部分 | 10 字节 |

**核心函数**：

| 函数 | 做什么 | 用在哪儿 |
|------|--------|---------|
| `dns_decode_query()` | 解析请求报文 → Header + 域名 + 类型 | 每次收到客户端查询 |
| `dns_encode_name()` | 域名编码 | 构造响应/转发时 |
| `dns_decode_name()` | 域名解码（支持压缩指针） | 解析查询/响应时 |
| `dns_build_response_multi()` | **构造响应报文**（4 种类型） | 本地解析/拦截时 |
| `dns_build_relay_query()` | 构造转发报文（替换 ID） | 中继转发时 |
| `dns_restore_id()` | 恢复响应中的 ID | 收到上游响应时 |
| `dns_extract_a_record()` | 从响应中提取 A 记录的 IP | 缓存时 |
| `dns_extract_ttl()` | 从响应中提取第一个 Answer 的 TTL | 缓存时 |

**关键设计**：所有多字节整数用 `htons/htonl/ntohs/ntohl` 转换字节序。

---

## src/dns_table.h / dns_table.c — 数据管理层

**功能**：把 `dnsrelay.txt` 读进内存哈希表，提供 O(1) 域名查找。

**数据结构**：

```c
DNSTable
  └── buckets[8191]           ← 哈希桶数组
        └── TableEntry        ← 链表（链地址法解决碰撞）
              ├── domain: "www.bupt.cn"
              ├── type: 1/28/5/15/2/12
              ├── ip: 0x0A7F8632 (A 记录的 IP)
              └── data: "www.baidu.com" (CNAME 目标)
```

**函数**：

| 函数 | 做什么 |
|------|--------|
| `dns_table_init(t, size)` | 分配哈希桶 |
| `dns_table_load(t, filename)` | 读文件进哈希表 |
| `dns_table_lookup(t, domain, &ip)` | 查第一个 IP |
| `dns_table_lookup_all(t, domain, ips, max)` | 查所有 IP（一域名多 IP） |
| `dns_table_lookup_type(t, domain, &type, &ip, data)` | 查记录类型 + 值 |
| `dns_table_destroy(t)` | 释放内存 |

**算法**：DJB2 哈希 + 键地址法 + 头插法。

---

## src/dns_cache.h / dns_cache.c — 数据管理层

**功能**：缓存完整 DNS 响应报文，TTL 过期自动清理。

**数据结构**：

```c
DNSCache
  └── buckets[1021]
        └── CacheEntry
              ├── key: "baidu.com|1"       ← 域名|类型
              ├── response: 完整 DNS 报文
              ├── response_len: 93
              └── expiry_time: time(NULL) + TTL
```

**函数**：

| 函数 | 做什么 |
|------|--------|
| `dns_cache_init(c, size)` | 初始化缓存 |
| `dns_cache_put(c, key, resp, len, ttl)` | 存完整报文 |
| `dns_cache_get(c, key)` | 取缓存（返回 CacheEntry*） |
| `dns_cache_evict_expired(c)` | 清理过期条目（二级指针遍历） |
| `dns_cache_destroy(c)` | 释放内存 |

**关键设计**：缓存 key 为 `域名|类型`，A 和 AAAA 分开存，互不覆盖。

---

## src/id_map.h / id_map.c — 业务逻辑层

**功能**：管理转发 ID 映射，支持多客户端并发查询。

**数据结构**（数组，1024 个槽位）：

```c
IDMap
  └── entries[1024]
        └── IDMapEntry (in_use=1)
              ├── orig_id: 客户端原始 ID
              ├── relay_id: 分配的转发 ID
              ├── client_addr: 客户端地址
              └── send_time: 发送时间
```

**函数**：

| 函数 | 做什么 |
|------|--------|
| `id_map_init(m, capacity)` | 初始化 |
| `id_map_alloc(m, orig_id, client, &relay_id)` | 分配唯一 relay_id |
| `id_map_lookup(m, relay_id, &orig_id, &client)` | 根据 relay_id 找客户端 |
| `id_map_free(m, relay_id)` | 释放条目 |
| `id_map_cleanup(m, timeout)` | 清理超过 5 秒的条目 |

**关键设计**：do-while 循环检查 relay_id 不与其他条目冲突。

---

## src/dns_relay.h / dns_relay.c — 业务逻辑层

**功能**：把客户端查询转发到上游 DNS。

**函数**：

| 函数 | 做什么 |
|------|--------|
| `relay_forward(sock, dns_server, query, len, client, map)` | 分配ID→替换ID→sendto上游 |

**流程**：
```
① dns_decode_query()      解析查询 → 原始 ID + 域名
② id_map_alloc()          分配 relay_id，记录客户端
③ dns_build_relay_query() 构建新报文（ID=relay_id）
④ sendto(sock)            发往上游 DNS
```

---

## 根目录文件

| 文件 | 说明 |
|------|------|
| `dnsrelay.exe` | 编译好的可执行程序 |
| `dnsrelay.txt` | 域名-IP 对照表（手动配置拦截/本地解析规则） |
| `Makefile` | 编译（`make` / `make clean`） |
| `操作手册.md` | 怎么编译、运行、测试 |
| `代码导读.md` | 每个模块/函数做什么 + 响应报文构造详解 |
| `课程设计报告.md` | 完整实验报告（含协议讲解、算法、调试记录） |
| `验收问答准备.md` | 验收问答 + 代码位置 |
| `README.md` | 项目简介 |
