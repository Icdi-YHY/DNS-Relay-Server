# DNS 中继服务器 — 验收问答准备

> 模拟验收老师可能问到的问题及答案，按主题分类。

---

## 一、DNS 协议基础

### Q1: DNS 报文头部有几个字段？各占多少字节？

**A:** 6 个字段，共 **12 字节**：

| 字段 | 字节数 | 说明 |
|------|:------:|------|
| ID | 2 | 查询标识符，客户端设置，响应中原样返回 |
| Flags | 2 | 标志位（QR/OPCODE/AA/TC/RD/RA/Z/RCODE） |
| QDCOUNT | 2 | Question 段中的问题数 |
| ANCOUNT | 2 | Answer 段中的资源记录数 |
| NSCOUNT | 2 | Authority 段中的记录数 |
| ARCOUNT | 2 | Additional 段中的记录数 |

### Q2: RCODE 有哪些值？各表示什么？

**A:**

| RCODE | 含义 | 说明 |
|:-----:|------|------|
| 0 | NOERROR | 成功，正常返回 |
| 1 | FORMERR | 查询格式错误 |
| 2 | SERVFAIL | 服务器故障 |
| 3 | **NXDOMAIN** | **域名不存在（拦截时返回这个）** |
| 5 | REFUSED | 拒绝查询 |

### Q3: NXDOMAIN 是什么？什么时候返回？

**A:** NXDOMAIN 是 RCODE=3，表示"域名不存在"。在拦截功能中使用——当 `dnsrelay.txt` 中域名对应 IP 为 `0.0.0.0` 时，返回 NXDOMAIN，不返回 IP 地址。

### Q4: DNS 名称压缩指针是什么？怎么工作的？

**A:** 压缩指针是 DNS 协议为了节省空间的一种机制。当报文中同一个域名出现多次时（如 Question 段和 Answer 段），后面的会用指针指向前面的位置。如果字节的最高两位是 `11`（即 `>= 0xC0`），该字节和后一个字节拼成 14 位偏移量，指向报文中的另一个位置。

---

## 二、程序实现

### Q5: 拦截功能是怎么实现的？

**A:** 当 `dnsrelay.txt` 中域名对应的 IP 为 `0.0.0.0` 时，程序检测到 `ip == 0`，调用 `dns_build_response_multi(nxdomain=1)` 构造响应报文，设置 **RCODE=3、ANCOUNT=0**，告诉客户端"域名不存在"。**不是返回 0.0.0.0 这个 IP**。

### Q6: ID 映射是怎么做的？为什么需要？

**A:** 多个客户端可能使用相同的原始查询 ID，直接转发会导致冲突。程序用 `id_map_alloc` 给每个查询分配一个**唯一**的 `relay_id`（从 1 开始递增），记录映射 `relay_id → (原始ID, 客户端地址)`。收到上游响应时，用 `relay_id` 查 `id_map_lookup` 找到对应的客户端，还原原始 ID 后发回去。

```
客户端A 原始ID=0x0001 → 分配 relay_id=1 → 问上游(ID=1)
客户端B 原始ID=0x0001 → 分配 relay_id=2 → 问上游(ID=2)
上游返回 ID=1 → 查表 → 还给 A
上游返回 ID=2 → 查表 → 还给 B
```

### Q7: 缓存存的是什么？存在哪里？

**A:** 存的是**完整的 DNS 响应报文**（`uint8_t *response`），存在**内存哈希表**（`dns_cache`）里。不写回 `dnsrelay.txt` 文件，程序退出就释放。命中时直接 `sendto` 回放，不用重新构造报文。

只有上游返回 RCODE=0 的成功响应才会被缓存，TTL 最小 60 秒。

### Q8: 超时处理怎么做的？

**A:** `select` 每 1 秒超时一次，调用两个清理函数：
- `id_map_cleanup()` — 清理超过 5 秒没收到上游响应的挂起查询
- `dns_cache_evict_expired()` — 清理过期的缓存条目

超时不通知客户端（符合 DNS 协议透明性），客户端侧自然超时重试。

---

## 三、网络编程

### Q9: 为什么用 select 而不是多线程？

**A:** 本程序是单线程事件驱动模型。`select` 同时监控 socket，有事件时处理，没有时阻塞。不需要锁，不会出现竞态条件，实现简单。适合 DNS 这种短请求-响应的场景。

### Q10: 一个 socket 怎么区分客户端查询和上游响应？

**A:** 收到数据报后检查**源地址**：
- 源地址 == 上游 DNS 地址 → 上游响应，调用 `handle_upstream_response`
- 否则 → 客户端查询，调用 `handle_client_query`

### Q11: 为什么用 1 个 socket 而不是 2 个？

**A:** UDP 是无连接的，一个 socket 可以同时与多个地址通信。1 个 socket 实现更简单，`select` 只监控 1 个 fd，效率更高。收到数据时通过源地址区分即可。

---

## 四、设计决策

### Q12: 一个域名多个 IP 怎么实现的？

**A:** 在 `dnsrelay.txt` 中写多行同一个域名：

```
123.127.134.10 www.bupt.cn
202.108.33.89 www.bupt.cn
```

程序加载时，会在哈希表的同一个桶中创建多个 `TableEntry`。查询时 `dns_table_lookup_all` 遍历所有条目，返回全部 IP。`dns_build_response_multi` 在 Answer 段写多条 A 记录。

### Q13: 支持哪些其他查询类型？

**A:** 支持 A、AAAA、CNAME、MX、NS、PTR 六种类型。文件格式：

```
# A 记录（默认）
123.127.134.10 www.bupt.cn

# 扩展格式：TYPE 值 域名
AAAA 2001:da8:215::1 ipv6.bupt.cn
CNAME www.bupt.cn alias.bupt.cn
MX mail.bupt.cn mx.bupt.cn
NS ns1.bupt.cn myns.bupt.cn
PTR www.bupt.cn 1.2.3.4.in-addr.arpa
```

### Q14: 哪里用了 AI？

**A:** 部分代码框架和文档由 AI 辅助生成。但核心逻辑（DNS 报文编解码、ID 映射、哈希表实现、缓存管理、事件循环）是自己实现的，完全理解每一行代码的作用。

---

## 五、运行与测试

### Q15: 怎么启动程序？

```powershell
.\dnsrelay -d 10.3.9.5 dnsrelay.txt
```

- `-d`：调试模式
- `10.3.9.5`：上游 DNS 服务器 IP
- `dnsrelay.txt`：域名-IP 对照表文件

### Q16: 怎么测试各功能？

| 功能 | 命令 | 预期输出 |
|------|------|---------|
| 拦截 | `nslookup ad1.sina.com.cn 127.0.0.1` | `Non-existent domain` |
| 本地解析 | `nslookup www.bupt.cn 127.0.0.1` | 返回 IP |
| 中继 | `nslookup www.baidu.com 127.0.0.1` | 返回真实 IP |
| 缓存 | 重复查中继过的域名 | 第二次显示 `缓存命中` |
| AAAA | `nslookup -type=AAAA ipv6.bupt.cn 127.0.0.1` | 返回 IPv6 |
| CNAME | `nslookup alias.bupt.cn 127.0.0.1` | 别名 + IP |
| MX | `nslookup -type=MX mx.bupt.cn 127.0.0.1` | 邮件服务器 |
| 多客户端 | 两台电脑同时查 | 日志显示两个不同 IP |

### Q17: Wireshark 看什么？

过滤条件：`udp.port == 53`

| 测试 | 看什么 |
|------|--------|
| 拦截 | RCODE=3, Answers=0 |
| 本地解析 | RCODE=0, Answer 里有 IP |
| 中继 | 两个查询包的 Transaction ID **不同** |
| 缓存 | 第二次只有 2 个包，没有去上游的包 |
| 多客户端 | 两个不同的源 IP 地址 |

---

## 六、常见问题

### Q18: 为什么 nslookup 查某些域名显示空白？

因为默认 `nslookup` 只查 A+AAAA 类型。如果表里配的是 MX/NS/PTR 类型，需要加 `-type=XXX` 指定类型才能看到结果。

### Q19: 为什么在校园网外无法中继？

因为上游 DNS 设的是校园网 DNS（如 `10.3.9.5`），在校外不可达。回到校园网即可正常使用，或者改为公共 DNS（如 `8.8.8.8`）。

### Q20: 缓存会写回 dnsrelay.txt 吗？

**不会。** 缓存只在内存中（`dns_cache` 哈希表），程序退出就释放。`dnsrelay.txt` 只保存你手动配置的规则，不会被程序自动修改。
