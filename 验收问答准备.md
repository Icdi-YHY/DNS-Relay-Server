# 验收模拟问答（含代码位置）

> 每个答案末尾的 📍 标记了对应代码的具体位置，方便老师追问时快速定位。

---

## 拦截功能

**老师**：拦截功能怎么实现的？

**你**：读 `dnsrelay.txt` 时，如果 IP 是 `0.0.0.0`，`inet_pton` 会把它转成 `entry->ip = 0`。查询时 `dns_table_lookup_type` 返回 `type=1, ip=0`，`main.c` 判断 `ip == 0` 就走拦截分支，调用 `dns_build_response_multi` 设 `nxdomain=1`，构造 RCODE=3、ANCOUNT=0 的响应，告诉客户端"域名不存在"。

📍 `dns_table.c:77-78` 加载时 `inet_pton("0.0.0.0")` → `entry->ip = 0`
📍 `main.c:343-351` 判断 `ip == 0` → `nxdomain=1` → RCODE=3
📍 `dns_message.c:168-171` `nxdomain=1` 时设 RCODE=3、ANCOUNT=0

---

## 本地解析（一域名多 IP）

**老师**：一个域名多个 IP 怎么配的？

**你**：在 `dnsrelay.txt` 里写多行同一个域名就行：

```
123.127.134.10 www.bupt.cn
202.108.33.89 www.bupt.cn
```

加载时每行独立插入哈希表。查询时 `dns_table_lookup_all` 遍历桶中所有条目收集全部 IP，`dns_build_response_multi` 在 Answer 段写多条 A 记录。

📍 `dns_table.c:151-168` `dns_table_lookup_all` 遍历同域名所有条目
📍 `dns_message.c:249-260` 写多条 A 记录（循环写 name_ptr + RR + IP）

---

## 多客户端 + ID 映射

**老师**：多客户端同时查询，ID 冲突怎么解决？

**你**：用 `id_map` 模块。每个查询进来时，`id_map_alloc` 分配一个唯一的 `relay_id`（从 1 递增），记录映射 `relay_id → (原始ID, 客户端地址)`。转发时把查询 ID 换成 `relay_id`。收到上游响应时，`id_map_lookup` 查 `relay_id` 找到对应的客户端和原始 ID，`dns_restore_id` 还原后发回去。两个客户端用相同原始 ID 也不会冲突。

📍 `id_map.c:29-67` `id_map_alloc` — 找空闲槽位、分配唯一 relay_id、记录映射
📍 `id_map.c:69-83` `id_map_lookup` — 根据 relay_id 查原始 ID 和客户端地址
📍 `dns_relay.c:49-53` 转发时 `dns_build_relay_query` 替换 ID
📍 `main.c:416-427` 收到响应后查表、还原 ID、发送给客户端

---

## 缓存

**老师**：缓存存在哪里？命中后还走中继吗？

**你**：存在内存哈希表 `dns_cache` 里，存的是**完整 DNS 响应报文**（`uint8_t *response`），不写回文件，程序退出就释放。每次查询**最先查缓存**，命中就直接回放，不走中继。

📍 `main.c:319-330` 缓存命中判断：先 `dns_cache_get`，命中直接 `sendto` 返回
📍 `dns_cache.c:109-120` `dns_cache_get` — 哈希查找 + 过期检查
📍 `dns_cache.c:34-83` `dns_cache_put` — 存完整报文 + TTL 管理
📍 `main.c:455-460` 缓存写入条件：仅 RCODE=0 时存

---

## 超时处理

**老师**：超时怎么处理的？

**你**：`select` 每 1 秒超时一次，调 `id_map_cleanup` 清理超过 5 秒没收到响应的挂起查询，调 `dns_cache_evict_expired` 清理过期的缓存条目。超时不通知客户端（符合 DNS 协议透明性），客户端侧自然超时重试。

📍 `main.c:150-154` select 超时 → `id_map_cleanup` + `dns_cache_evict_expired`
📍 `id_map.c:97-116` `id_map_cleanup` — `difftime(now, send_time) >= 5` 释放条目
📍 `dns_cache.c:133-157` `dns_cache_evict_expired` — 二级指针遍历链表删过期

---

## 1-socket 架构

**老师**：为什么用 1 个 socket？

**你**：UDP 是无连接的，一个 socket 可以同时与多个地址通信。收到数据时通过源地址区分是客户端还是上游响应。比 2 个 socket 更简单，select 只监控 1 个 fd，效率更高。

📍 `main.c:170-181` 1-socket 源地址分发：
```c
if (源地址 == 上游DNS地址) → handle_upstream_response()
else → handle_client_query()
```

---

## 输出格式

**老师**：输出格式有哪些内容？

**你**：`[时间] [序号] 查询: 域名 类型=XX 客户端=IP`，然后根据情况显示 `→ 拦截`、`→ 本地解析`、`→ 中继`、`→ 缓存命中`、`→ 未缓存(RCODE=X)`。所有输出通过 `DEBUG(level, fmt, ...)` 宏，写到 stderr，不显示报文二进制内容。

📍 `main.c:317` 查询日志格式：`DEBUG(1, "查询: %-30s 类型=%3u  客户端=%-15s", ...)`
📍 `debug.h:43-50` `DEBUG` 宏定义：`fprintf(stderr, "[%s] [%d] " fmt "\n", ...)`

---

## DNS 协议基础

**老师**：DNS 报文头部几个字段？

**你**：6 个字段，共 12 字节：ID（2B）、Flags（2B）、QDCOUNT（2B）、ANCOUNT（2B）、NSCOUNT（2B）、ARCOUNT（2B）。

📍 `dns_message.h:24-31` `DNSHeader` 结构体定义

**老师**：RCODE=3 是什么？

**你**：NXDOMAIN，表示"域名不存在"。拦截功能就是返回这个。

**老师**：压缩指针是什么？

**你**：DNS 中如果同一个域名出现多次，后面用指针指向前面的位置。如果字节 `>= 0xC0`，和后一个字节拼成 14 位偏移量，跳转到报文中的另一个位置继续读域名。

📍 `dns_message.c:14-22` `dns_decode_name` 中 `if ((*p & 0xC0) == 0xC0)` 处理压缩指针

---

## 其他类型查询

**老师**：除了 A 记录还支持什么类型？

**你**：AAAA、CNAME、MX、NS、PTR。文件格式用类型前缀：

```
AAAA 2001:da8:215::1 ipv6.bupt.cn
CNAME www.bupt.cn alias.bupt.cn
MX mail.bupt.cn mx.bupt.cn
```

📍 `dns_table.c:15-25` `parse_type` 识别类型前缀
📍 `dns_message.c:271-312` `dns_build_response_multi` 根据类型写不同 RDATA

---

## 模块划分

**老师**：程序分几个模块？

**你**：9 个 .c/.h 文件，每个模块只做一件事：

| 模块 | 职责 |
|------|------|
| `platform` | socket 初始化/关闭，跨平台 |
| `debug` | 调试输出宏 + 序号 |
| `util` | 域名编解码/IP 转换工具 |
| `dns_message` | DNS 报文编解码 |
| `dns_table` | 域名-IP 哈希表 |
| `dns_cache` | 缓存完整响应报文 |
| `id_map` | ID 映射管理 |
| `dns_relay` | 中继转发 |
| `main.c` | 串联所有模块，事件循环 |

---

## DNS 协议专题

### Q: DNS 使用什么传输层协议？为什么？

**A:** 主要用 **UDP**，端口 53。因为 DNS 查询通常很短（请求和响应都在 512 字节以内），UDP 无需建立连接，效率更高。当响应超过 512 字节时，DNS 会切换到 TCP。

📍 代码中 `SOCK_DGRAM` 创建 UDP socket

### Q: DNS 报文最大长度是多少？

**A:** 512 字节（RFC 1035 规定）。超过时响应中设 TC=1 标志，客户端用 TCP 重试。

📍 `platform.h:53` `#define MAX_DNS_PACKET 512`

### Q: 递归查询和迭代查询的区别？

**A:** 
- **递归查询**：DNS 服务器必须返回最终答案（要么是 IP，要么是"不存在"），不能返回 referral
- **迭代查询**：DNS 服务器返回"我不知道，你去问这个服务器"
- 我们的中继服务器相当于做**递归查询**——客户端问我们，我们替它去问上游，拿到最终答案返回。

### Q: 什么是 DNS 缓存？有什么好处？

**A:** 把查询过的结果暂存在内存里。第二次查同一个域名时直接返回，不用再问上游，**减少网络延迟和上游负载**。我们的缓存还带 TTL 过期自动清理。

📍 `dns_cache.c` 完整缓存实现

### Q: DNS 的 TTL 是什么？

**A:** Time To Live，告诉客户端/缓存这个记录能存多久（秒）。TTL 到了就必须重新查询，确保拿到最新的 IP。我们的程序支持从上游响应提取真实 TTL，或用 `-ttl` 参数手动指定。

📍 `dns_cache.c:38` `time_t expire = time(NULL) + ttl`

### Q: 为什么要做 ID 转换？不转换会怎样？

**A:** 两个客户端可能用相同的原始 ID（比如 0x0001）。直接转发会导致冲突——上游返回 ID=0x0001 时，不知道该给谁。转换后每个查询有唯一的 relay_id，响应回来时查表找到正确的客户端。

📍 `id_map.c:29-67` `id_map_alloc`

### Q: 什么是 DNS 劫持？

**A:** 网络设备拦截你的 DNS 请求，不让你去问真正的 DNS 服务器，而是返回假的响应。之前我们的程序从 53 端口发查询被校园网返回 RCODE=1，就是典型的劫持现象。

### Q: CNAME 记录和 A 记录有什么区别？

**A:** A 记录直接返回 IP 地址。CNAME 记录返回另一个域名（别名）。客户端收到 CNAME 后需要再去查那个域名的 A 记录。我们的程序在返回 CNAME 时会**同时附带**目标域名的 A 记录，减少一次查询。

📍 `dns_message.c:274-298` CNAME + 附加 A 记录

### Q: 什么是 DNSSEC？我们的程序支持吗？

**A:** DNSSEC 是 DNS 的安全扩展，对响应进行数字签名，防止伪造。我们的程序**不支持** DNSSEC 验证，只做透明转发。这是课程设计范围外的内容。

### Q: 一个 DNS 响应中可以有多个 Answer 吗？

**A:** 可以。一个域名对应多个 IP 时，Answer 段会有多条 A 记录。`ANCOUNT` 字段记录 Answer 的数量。我们的程序用 `dns_build_response_multi` 循环写多条记录。

📍 `dns_message.c:249-260` 循环写多条 A 记录

---

## AI 相关

**老师**：哪些地方用了 AI？

**你**：部分代码框架和文档由 AI 辅助生成。但核心逻辑（DNS 报文编解码、ID 映射、哈希表、缓存、事件循环）是自己实现的，完全理解每一行代码的作用。
